# 放缩法 V2 出题器规则（当前唯一有效版本）

> 本文件覆盖此前关于“整百/特殊基准/倍数型按 40/35/25 作为一级配额”的设计。
> 旧版“整百放缩修正”直接删除，不做历史兼容。

## 1. 一级配额：按资料分析计算来源分配

放缩法首先模拟资料分析中实际遇到的除法来源，而不是先按基准数类型凑题。

- 求基期：45%
- 比值 / 比重：35%
- 倍数：20%

内部场景 key：

- `base_period`
- `ratio_share`
- `multiple`

基准类型只作为二级结构标签，例如：

- `round_baseline`：整十 / 整百等常规友好基准
- `special_baseline`：125、143、167、250、333 等特殊友好基准
- `relation_baseline`：利用分子分母数值关系得到的友好基准

二级结构不设固定 40/35/25 硬配额。生成器应先保证题目来自正确场景且确实值得用放缩法，再记录实际使用了哪类基准。

## 2. 求基期场景

求基期是 45% 的主场景，重点模拟：

`现期 ÷ (1 + 增长率)`

整数化后重点表现为“较大现期数 ÷ 一百多”的结构，例如分母 106、113、127、143、167、186 等。

生成器不能机械把所有分母放到 100～199；这是求基期场景的高频特征，不是整个放缩法题型的统一位数限制。

## 3. 比值 / 比重场景

两个统计量直接相比。分子分母量级可以更自由，核心约束是：

- 存在 10% 左右范围内有实战价值的友好基准；
- 放缩后比直接继续除明显更容易；
- 不能靠首位商直接排除大多数选项。

## 4. 倍数场景

本质仍是两个统计量相除。该场景表示资料分析题目的“求几倍”，不要与 `relation_baseline` 混为一谈。

倍数场景可以产生 2.x、3.x 等结果，因此选项位数采用有效数字规则，而不是强制结果为三位/四位整数。

## 5. 选项精度

每题四个选项统一采用：

- 3 位有效数字，或
- 4 位有效数字。

例如：

- `2.30` 是 3 位有效数字；
- `16.00` 是 4 位有效数字；
- `161.4` 是 4 位有效数字。

生成器保存数值和展示字符串，展示字符串必须保留必要的尾零。

## 6. 四个选项首位必须相同

四个选项的首个有效数字必须完全相同。

例如可以是：

- 156.9
- 160.8
- 161.4
- 171.1

首位都为 `1`。

原因：若四个选项首位不同，用户做一步普通除法、只估首位商就可能直接得出答案，无法训练放缩与修正。

因此选项生成必须在舍入后再次校验：

1. 四项互不重复；
2. 正确答案唯一存在；
3. 四项有效数字位数一致；
4. 四项首个有效数字一致；
5. 若 Q、R1、错误方向结果跨越首位边界，则该候选不能直接作为干扰项，需换用同首位的邻近诊断选项。

## 7. 当前代码对应

`src/lib/scaling-method.ts`：

- `SCALING_METHOD_SCENARIO_QUOTAS` 固化 45/35/20；
- `ScalingStructure` 仅作为二级基准结构；
- `buildScalingOptions()` 强制 3/4 位有效数字和同首位；
- `formatScalingOptionValue()` 保留有效数字要求所需尾零；
- `leadingSignificantDigit()` 用于选项首位约束。

下一步是在 `generate.ts` 中按场景配额真正生成完整题组，并将旧 `special_hundred_scaling_division` 生成链路删除。
