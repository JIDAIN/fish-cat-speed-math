# 速算训练前台产品结构重构方案

> 状态：**方案阶段，尚未实施**  
> 日期：2026-09-08  
> 决策依据：[ADR-001：前台按训练任务/方法组织，微能力留在后台诊断](./docs/adr/ADR-001-student-facing-training-units.md)

## 1. 这次要解决的问题

当前程序已经建立 160 个纯计算叶子能力，并能按 `skill_id + difficulty_band` 记录、诊断和推荐。但手机真实使用发现：

- 160 个微能力如果直接作为学生专项，学生面对的是后台诊断树，而不是实际训练任务；
- 即使先收成若干大类，再展开“试商、求余、误差传播、运算顺序、基准选择”等子能力，仍然需要做过多选择；
- 学生真正想提高资料分析速算时，主动选择的只有少量计算类型或完整方法；
- “连续进位、试商回退、求余量、误差停止、r、一阶修正”等更适合作为程序后台识别的薄弱点，而不是前台训练目录。

因此，下一版前台要把**学生可见训练项目**和**后台诊断能力**彻底分层。

---

## 2. 核心产品原则

### 原则 A：前台按“训练任务 / 完整方法”组织

学生只需要回答：

> **我现在想练什么计算？**

而不是：

> 我现在想练哪个内部能力节点？

160 个叶子能力继续保留，但定位改为后台的：

- 题目结构控制；
- 每题/每步标签；
- 分步计时节点；
- Mastery 聚合单位；
- Diagnostic 下钻目标；
- 自动弱项强化的生成依据。

### 原则 B：不同训练目标使用不同交互

交互形式不追求统一，而根据训练目标决定：

1. **反应型训练 → 选择题 / 快速点选**  
   目标是极快反应，避免输入动作本身污染反应时间。

2. **结果计算型训练 → 数字填入**  
   目标是完整算出结果，保留真实计算耗时。

3. **完整方法型训练 → 单界面结构化多输入**  
   一个方法内部可以包含很多微能力，但学生只进入一个“方法训练”。程序在多个关键输入背后记录对应 `skillId / stepSkillId`。

### 原则 C：弱项由程序发现，不要求学生主动寻找

学生正常完成训练后，程序从后台判断：

- 哪里容易错；
- 哪里“会但慢”；
- 哪种数字结构反复拖慢；
- 哪个完整方法中的哪一步是瓶颈。

前台只给自然语言结论与 1～2 个建议，再由“弱项强化”自动增加对应结构权重。

### 原则 D：完整方法暴露瓶颈，基础训练消灭瓶颈

同一个底层能力可以同时存在于两种场景：

- 在完整方法中被自然触发并记录耗时；
- 当它确实需要独立高频训练时，由基础训练或弱项强化专门练习。

例如：

- “邻近倍数反应”既是独立的秒选训练，也是直除试商的底层能力；
- “数 × r 的修正量乘法”属于乘法训练中的一种结构，同时也是放缩法一阶修正的核心瓶颈。

这种重叠是有意保留的，不等于重复建立两套能力。

---

## 3. 三层架构边界保持不变

### 第一层：纯计算能力

当前程序主要开发这一层。

前台只呈现纯计算任务和第一层完整计算路线；后台仍使用 160 个微能力诊断。

### 第二层：资料分析专用计算方法

后续单独开发，例如：

- 415 / 份数法；
- 假设分配；
- 资料分析意义上的化除为乘；
- 增长率复合、十字交叉、年均增长率等。

这些方法同样遵守“一个方法 = 一个完整训练界面”，而不是把每个步骤做成学生菜单。

### 第三层：资料分析实战决策

后续才加入：

- 真实材料/目标量；
- 方法选择；
- 选项距离；
- 计算到什么精度；
- 同题路径决策。

用户此前设计的“放缩法 + 一阶/二阶 + 选项判断”界面属于**第三层完整实战界面的参考**。当前第一层只借鉴“一个方法放在一个完整界面中”的产品思想，不提前引入真实选项判断。

---

## 4. 第一层学生可见专项：当前冻结方案

当前先冻结为 6 个学生可见项目。它们不是 160 能力树的重新命名。

### 4.1 邻近倍数反应

目标不是背 `4×5=20` 或 `4×□=20`，而是看到类似：

`23 ÷ 4`

能够瞬间想到最接近的倍数 `24 = 4×6`，直接选择商 `6`，而不是从 3、4、5、6 逐个试。

典型题面：

`23 ÷ 4 ≈ ?`

候选：`4 / 5 / 6 / 7`

交互：**选择题 / 点选即提交**。

规则：

- 重点记录从题目出现到第一次点选的反应时间；
- 选项围绕真实商附近生成，不放明显无关的垃圾选项；
- 严格中点题默认避开，例如 `22÷4` 对 5 和 6 等距，避免单选歧义；
- 该能力与直除里的“试商”有意重叠：独立训练负责秒选，完整直除负责在真实方法中测它是否拖慢。

### 4.2 百化分反应

目标：固定分数与百分数关系形成快速双向检索。

例如：

`1/7 ≈ ?`

候选来自相邻高价值关系，如：`12.5% / 14.3% / 16.7% / 20%`。

反向同理：

`16.7% ≈ ?`

交互：**选择题 / 点选即提交**。

干扰项优先来自相邻固定关系，避免靠数量级轻易排除。

### 4.3 加减法

目标：资料分析列式后的完整加减计算速度。

前台只展示算式，用户直接输入最终结果。

交互：**数字填入**。

程序后台自动混入：

- 两位 / 三位 / 多项；
- 单次进借位 / 连续进借位；
- 跨 0；
- 凑整、拆分等不同结构。

学生不再主动选择这些微结构。

### 4.4 乘法

目标：训练完整乘积计算，同时覆盖资料分析速算中高频的修正量乘法。

前台仍只有一个“乘法”项目，后台自动混合两类结构：

1. 普通乘法，例如 `47×36`；
2. 修正量乘法，例如 `171.1×6%`、`3260×4%`。

交互：**数字填入**。

其中 `数 × r` 不单独占一个长期菜单项，但要作为乘法的重要结构保存。原因是：

- 结果端放缩往往真正慢在 `Q0 × r` 的一阶修正量；
- 分子端放缩也常慢在“基准量 × r”的修正乘法；
- 后台应区分结果端 / 分子端来源，但学生前台仍只是在练“乘法”；
- 如果完整放缩训练诊断出该步骤慢，弱项强化可以临时显示“乘法 · 修正量强化”，自动集中生成 `数 × r` 题。

### 4.5 除法

“除法”是唯一允许在学生前台进一步选择完整计算路线的项目。

当前保留 4 种模式：

1. **普通除法**；
2. **直除完整流程**；
3. **除法拆分 / 包子法**；
4. **除法补偿放缩**。

#### 普通除法

给出类似：

`53842 ÷ 317 ≈ ?`

用户只填最终结果，不规定使用哪种内部路径。

交互：**数字填入**。

#### 直除完整流程

学生明确训练“直除”这条方法时使用。

交互：**单界面结构化多输入**。

可能涉及商区间、试商、调商、余量、下一位、精度停止等后台节点，但正式 UI 不把它们拆成多个专项。具体哪些中间量值得让学生显式填写，实施前再单独精简设计。

#### 包子法

交互：**单界面结构化多输入**。

一题中自然承载主块、剩余量、下一块、尾量、累计结果和停止等关键量，后台分别记录步骤耗时。

#### 放缩法

交互：**单界面结构化多输入**。

第一层纯计算版可在同一题中承载：

`基准 → r → 粗算 / Q0 → 一阶修正量 → 一阶结果 → 必要时二阶`

不加入真实资料分析材料和 A/B/C/D 选项判断。

### 4.6 分数比较

目标：快速形成两个分数的大小判断。

交互：**选择 / 点选即提交**，只需要两个大按钮：

`>` / `<`

后台继续分析同向变化、接近基准、交叉量级、接近程度等结构。

---

## 5. 三种交互的产品规范

### 5.1 选择型

适用于：

- 邻近倍数反应；
- 百化分反应；
- 分数比较。

规范：

- 不弹数字键盘；
- 不额外点击“确定”；
- 点击即提交；
- 重点记录首选反应时间；
- 干扰项必须有训练价值；
- 适用于目标是“秒反应”的任务。

### 5.2 填入型

适用于：

- 加减法；
- 乘法；
- 普通除法。

规范：

- 一题一个主要数值答案；
- 使用数字键盘；
- 点击确定提交；
- 可按题型设置精度容忍；
- 主要统计完整计算时间；
- 不要求用户显式填写内部过程。

### 5.3 结构化多输入型

适用于：

- 直除完整流程；
- 包子法；
- 放缩法。

规范：

- **同一题、同一界面完成一个完整方法**；
- 多个输入框是一个方法链，不是多个专项；
- 可以记录每个关键输入/步骤的耗时和正误；
- 可以根据题目动态省略不需要的字段；
- 第一层不出现真实资料分析材料和选项决策；
- UI 优先参考用户已有放缩法设计的“一屏呈现完整关系”思路，而不是强制做成连续“下一步”的分步向导。

因此：

> **结构化多输入 ≠ 把方法拆成很多页面。**

---

## 6. 学生项目与后台能力的关系

这次重构不废弃 160 能力。

建立关系：

`studentTrainingProject → 多个 skillId / stepSkillId / structureTags`

例如：

### 邻近倍数反应 / 直除

- 邻近倍数反应作为独立快速选择项目；
- 直除过程中同样记录试商相关能力；
- 如果完整直除显示试商明显慢，弱项强化可以自动调用邻近倍数反应训练。

### 乘法 / 放缩

- 乘法项目中包含 `数 × r` 修正乘法；
- 放缩完整训练在一阶修正步骤继续记录同类底层能力；
- 如果放缩整体慢点集中在修正量乘法，弱项强化自动提高这类乘法题占比。

### 包子法

前台只有一个方法项目，后台继续记录第一主块、主块计算、剩余量、下一块、尾量、累计百分比、继续/退出和精度停止。

所以能力树从“菜单结构”回到真正的**诊断基础设施**。

---

## 7. 首页 / 专项页建议结构

### 首页

核心训练入口暂定：

1. **今日训练**；
2. **专项训练**；
3. **弱项强化**；
4. 历史 / 成绩 / PK 等已有功能继续独立存在。

“经典训练”只作为兼容旧入口，后续可以收到“更多”中。

### 专项训练页

只显示当前 6 个学生项目：

- 邻近倍数反应；
- 百化分反应；
- 加减法；
- 乘法；
- 除法；
- 分数比较。

进入“除法”后，才展示：

- 普通除法；
- 直除完整流程；
- 包子法；
- 放缩法。

不再展示 A/B/C、L1/L2/L3 微能力树、试商/求余/r/一阶/二阶等内部节点。

---

## 8. 数据和兼容策略

方案实施时遵守：

- 不删除 `SkillRegistry` 的 160 个正式叶子；
- 不改写旧 completed 会话；
- 旧 `skill_drill` 数据继续可读；
- Mastery / Diagnostic 的数据口径继续以微能力为基础；
- 新增学生项目层只负责“从哪里进入训练”和“如何组织题组”；
- 如新增 `trainingProjectId`，它应与 `skillId` 并存，不能取代 `skillId`；
- 旧 160 单能力入口可以保留为开发/诊断工具，但从学生正常 UI 隐藏。

---

## 9. 当前仍未定的问题

前两个设计问题已经冻结：

1. 第一层学生可见项目：当前 6 个；
2. 每个项目的交互类型：选择 / 填入 / 单界面结构化多输入已经确定。

正式改代码前还剩一个核心产品问题：

> **“今日训练”和“弱项强化”在历史数据不足时怎么工作、如何降级。**

当前仍不修改业务代码、不调整现网 UI、不部署。该问题定稿后，再进入前台重构实现。