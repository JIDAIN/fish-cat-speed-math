# 速算训练前台产品结构重构方案

> 状态：**方案阶段，尚未实施**  
> 日期：2026-09-08  
> 决策依据：[ADR-001：前台按训练任务/方法组织，微能力留在后台诊断](./docs/adr/ADR-001-student-facing-training-units.md)

## 1. 这次要解决的问题

当前程序已经建立 160 个纯计算叶子能力，并能按 `skill_id + difficulty_band` 记录、诊断和推荐。但首轮手机真实使用发现：

- 如果把 160 个微能力直接做成学生可选择的专项，学生面对的是“知识树/诊断树”，而不是自己实际会说的训练任务；
- 即使先收成九个大类，再展开“试商、求余、误差传播、运算顺序、基准选择”等子能力，手机端仍然需要做过多选择；
- 学生真正想提高资料分析速算时，主动选择的通常只是少量计算类型或完整方法，例如乘法、除法、分数百分数、包子法、放缩法；
- “试商过高回退、连续进位、求余量、误差停止”等更适合作为程序在后台识别的薄弱点，而不是前台训练目录。

因此，下一版前台不再沿“能力树 = 训练菜单”继续细化，而要把**学生可见训练项目**和**后台诊断能力**彻底分层。

---

## 2. 核心产品原则

### 原则 A：前台按“训练任务 / 完整方法”组织

学生只需要回答：

> **我现在想练什么计算？**

而不是：

> 我现在想练哪个内部能力节点？

160 个叶子能力继续保留，但定位改为后台的：

- 题目结构标签；
- 分步计时节点；
- Mastery 聚合单位；
- Diagnostic 下钻目标；
- 自动弱项强化的生成依据。

### 原则 B：不同训练目标使用不同交互

交互形式不追求统一，而根据训练目标决定：

1. **反应型训练 → 点选 / 选择题**  
   目标是极快反应，避免输入动作本身占掉主要时间。

2. **结果计算型训练 → 数字填入**  
   目标是完整算出结果，保留真实计算耗时。

3. **完整方法型训练 → 一个界面的结构化多输入**  
   一个方法内部可以包含很多微能力，但学生只进入一个“方法训练”。程序在各输入框/步骤背后记录对应 `skillId / stepSkillId`。

### 原则 C：弱项由程序发现，不要求学生主动寻找

学生正常完成训练后，程序从后台判断：

- 哪里容易错；
- 哪里“会但慢”；
- 哪种数字结构反复拖慢；
- 哪个完整方法中的哪一步是瓶颈。

前台只给自然语言结论与 1～2 个建议，例如：

- “除法：试商回退与求余偏慢”；
- “乘法：连续进位反应偏慢”。

随后由“弱项强化”自动提高对应结构占比，而不是让学生打开 160 项目录自己找节点。

---

## 3. 三层架构边界保持不变

### 第一层：纯计算能力

当前程序主要开发这一层。

前台只呈现纯计算任务和第一层完整计算路线；后台仍使用 160 个微能力诊断。

### 第二层：资料分析专用计算方法

后续单独开发，例如：

- 415 / 份数法；
- 假设分配；
- 资料分析意义上的化除为乘；
- 增长率复合、十字交叉、年均增长率等。

这些方法也遵守“一个方法 = 一个完整训练界面”，而不是把每个步骤做成学生菜单。

### 第三层：资料分析实战决策

后续才加入：

- 真实材料/目标量；
- 方法选择；
- 选项距离；
- 计算到什么精度；
- 同题路径决策。

用户此前设计的“放缩法 + 一阶/二阶 + 选项判断”界面可以作为**第三层完整实战界面的视觉与交互参考**。当前第一层/第二层可以借鉴“一个界面承载完整方法”的思想，但不能提前把资料分析选项判断塞进纯计算训练。

---

## 4. 第一层前台训练项目：V1 方案

以下是**学生可见项目**，不是 160 能力树的重新命名。

### 4.1 基础反应

目标：把高频关系练到接近条件反射。

建议项目：

- **乘法口诀**：正向与逆向合并为一个训练项目；
- **固定分数 / 百分数**：常用固定关系双向反应；
- **百分比块 / 常见倍数反应**：是否独立保留，等首轮方案评审后再定。

交互：**选择题 / 快速点选**。

例如 `4 × □ = 24` 不要求键盘输入 6，而是给出若干候选，训练反应时间。

### 4.2 基础计算

目标：直接提高资料分析列式后的纯计算速度。

建议项目：

- **加减法**；
- **乘法**；
- **普通除法**；
- **分数 / 数值比较**（是否与分数百分数合并，后续再定）。

交互：**数字填入 / 比较点选**。

程序在同一个“乘法”项目内自动混入不同进位结构；在“普通除法”内自动混入试商、求余、商位数等不同结构。

### 4.3 完整方法训练

目标：把一个完整计算路线练顺，而不是练某个孤立步骤。

第一层当前明确存在：

- **直除完整流程**；
- **除法拆分 / 包子法**；
- **除法补偿放缩**；
- **加减乘补偿性放缩**是否作为独立学生项目，后续结合实际使用价值再决定。

交互：**结构化多输入**。

例如放缩训练可以在同一屏中让用户依次填写：

`基准 → r → 粗算 / Q0 → 一阶修正 → 一阶结果 → 必要时二阶`

后台分别记录各字段/步骤对应的微能力耗时和正误。学生只会认为自己在做“放缩法训练”。

### 4.4 弱项强化

目标：把后台诊断直接转成下一轮训练，而不是再提供一棵可浏览能力树。

前台只显示：

- 当前最明显 1～2 个瓶颈；
- “开始弱项强化 10/20题”。

出题系统自动根据 `skillId / stepSkillId / structureTags / Mastery` 调整题目结构权重。

---

## 5. 首页 / 专项页的建议结构

### 首页

核心只保留：

1. **今日训练**：系统安排日常混合；
2. **专项训练**：用户明确选择一个计算项目/方法；
3. **弱项强化**：系统根据历史自动安排；
4. **历史 / 成绩 / PK**等已有功能继续独立存在。

“经典训练”如果需要兼容旧入口，可以放到“更多”中，不再占据核心训练层级。

### 专项训练页

不再展示 A/B/C 或 160 微能力。

只展示学生能理解的项目，例如：

- 乘法口诀
- 分数百分数
- 加减法
- 乘法
- 普通除法
- 直除完整流程
- 包子法
- 补偿放缩
- 数值比较

最终项目数在实施前再做一次删减评审，目标是**少而稳定**，而不是把现有 160 节点换一批中文名字继续铺开。

---

## 6. 后台如何继续利用 160 个能力

这次重构**不废弃能力树**。

建议建立新的映射概念：

`studentTrainingProject → 多个 skillId / stepSkillId / structureTags`

例如：

### “普通除法”

前台只有一个项目，后台可继续涉及：

- 商区间；
- 试商；
- 调商；
- 求余；
- 下一位；
- 首位 / 前两位；
- 精度停止。

### “包子法”

前台只有一个方法项目，后台继续记录：

- 适用判断；
- 第一主块；
- 主块计算；
- 剩余量；
- 下一块；
- 尾量；
- 累计百分比；
- 继续/退出；
- 精度停止。

### “补偿放缩”

前台只有一个方法项目，后台继续记录：

- 基准选择；
- 基准值得性；
- r；
- Q0；
- 一阶修正；
- 二阶必要性；
- 分子端/结果端路径等。

所以能力树从“菜单结构”变成真正的**诊断基础设施**。

---

## 7. 数据和兼容策略

方案实施时遵守：

- 不删除 `SkillRegistry` 的 160 个正式叶子；
- 不改写旧 completed 会话；
- 旧 `skill_drill` 数据继续可读；
- Mastery / Diagnostic 的数据口径继续以微能力为基础；
- 新增学生项目层只负责“从哪里进入训练”和“如何组织题组”；
- 如果需要新增 `trainingProjectId`，它应与 `skillId` 并存，不能取代 `skillId`；
- 旧的 160 单能力入口可以先保留为开发/诊断工具，但从学生正常 UI 隐藏，是否最终删除入口代码另行决策。

---

## 8. 这轮方案明确不做

当前只形成产品方案和 ADR，**不修改训练代码、不改变现网 UI、不部署**。

本轮不决定：

- 第二层每个资料分析方法的完整项目清单；
- 第三层真实材料题和选项决策界面；
- R/C/D/S/F 阈值；
- L1/L2/L3 最终数值范围；
- 自动弱项出题权重的最终参数。

---

## 9. 实施前还需要完成的设计确认

正式改代码前，只需要再定三个问题：

1. 第一层学生可见专项最终保留 **6～10 个中的哪几个**；
2. 每个项目属于 **快速选择 / 数字填入 / 结构化多输入** 中哪一种交互；
3. “今日训练”和“弱项强化”的初始自动组题规则，如何在没有足够历史数据时降级。

这三个问题定稿后，再进入前台重构实现。