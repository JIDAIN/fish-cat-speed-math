# 项目状态与变更记录

## 当前正式状态（2026-08-04）

- 第一阶段完成：多位数直除联合配额按数学可行性分配，生成器版本为 `2.6.0`；趋势按用户、题型、子模式和实际题量隔离。
- 第二阶段完成：Supabase Auth 固定 fish/cat 身份、RLS 配对可读、completed 幂等同步、云端/本地去重、旧 completed 历史显式合并或丢弃，以及同步状态与逐条手动重试。
- active 只保留本地：按账号或未登录本地范围隔离，每个范围最多一条；隐藏、失焦、锁屏、冻结、`pagehide`、返回/前进、关闭/跳转和恢复读取均暂停计时，BFCache 不补计离开间隔，账号切换不会接管其他账号 active。
- 最近验证：`format`、`typecheck`、`lint`（仅保留既有生成器未使用变量警告）、`test`（149 项）和 `build` 均通过。
- 等级评定重构：`2.0.0` 按题型 + 子模式配置资料分析导向的速度与正确题数双门槛，新 completed 冻结 `rating` 快照；完整标准见 [RATING_STANDARDS.md](./RATING_STANDARDS.md)。
- 历史记录与成绩汇总升级：历史页默认显示当前账号，可切换查看已配对对象的只读记录；筛选仅列出所选用户实际存在的题型和子模式。汇总按筛选范围计算训练组数、总题数、加权正确率、加权平均单题用时、最近/最佳等级和等级分布；趋势仍按实际题量分轨。统计口径见 [HISTORY_REPORTING.md](./HISTORY_REPORTING.md)。
- 最近验证：`format`、`typecheck`、`test`（150 项）通过；本轮随后执行 `lint` 与生产构建复核。
- 异步 PK 阶段：已新增冻结题组挑战、结果比较、近7日展示、首页红/蓝提醒、个人历史 PK 来源标签与传统分页；数据库迁移为 `supabase/migrations/20260804_async_pk.sql`，完整边界与验收见 [PK_ASYNC.md](./PK_ASYNC.md)。
- PK详情页复盘修正：移除与列表页重复的总览、筛选和胜负说明，只按冻结题组顺序展示完整逐题作答；稳定 ID 优先关联双方记录，缺失项仍保留并标注未作答。
- 刷新体验与首页收敛：产品名称统一为“速算训练”；账号初始化分为确认中/已登录/已确认未登录，未确认时不显示登录表单。hash 路由恢复独立页面、详情 ID、结算记录与本浏览器 active 训练；首页账号区改为按需登录的紧凑状态栏。规则见 [NAVIGATION_RECOVERY.md](./NAVIGATION_RECOVERY.md)。

> 下文为按日期保留的历史记录；与本节冲突的旧“未实现”描述不再代表当前状态。

## 九类题型生成逻辑复核（2026-08-03）

- 已按真实代码、生成器测试和项目文档逐项复核九类底层题型。两位数加减、三位数加减、两位数×一位数、两位数×两位数、四个三位数相加、三位数÷两位数、固定百化分转换和分数比较的正式题组规则与主要文档一致。
- 正式训练使用 `generateSet()` 分配结构配额；`generateQuestion()` 只用于单题生成与烟雾测试，个别题型的单题抽样策略不代表完整训练题组比例。文档现已明确该边界。
- 发现多位数直除的二级配额缺陷：五种商结构配额可以保持，但商结构与三/四/五位除数配额按固定索引配对，数学上不可行的组合会进入备用题，导致最终题面的除数位数无法保证 75%/15%/10%。现有测试只验证 100 题时的商结构，没有验证全部合法题量的最终位数分布。
- 本轮仅审查和修正文档，没有改变任何题目数字、答案、判题或结构比例。`DEVELOPMENT_PLAN.md` 已新增任务 1.8.1，要求先修复联合配额并补属性测试，再进入专项训练。
- 同步清理了三处过时描述：三位数÷两位数不可能出现商小于 1；固定百化分不再包含随机“接近基准/普通直除”；专项训练不再引用不存在的“两位数加法两次进位”或已删除的“普通分数直除”结构。

## 复盘草稿悬浮入口与图标工具栏（2026-08-03）

- 即时结算和历史详情共用的“草稿”入口改为避让安全区的悬浮按钮；题目明细继续正常滚动，滚动到任意后续错题时入口仍在当前可视区域内。
- 草稿底栏统一为三个图标按钮：左侧选择画笔颜色，中间清空整张画布，右侧关闭草稿；颜色面板提供深绿、黑、红、蓝四色。
- 画笔颜色冻结在每一笔数据上，切换颜色只影响后续笔画，不会重绘或改色已经写下的内容；草稿依旧只存在于内存，不进入训练记录或历史。
- 新增悬浮入口、安全区、三图标工具栏和颜色面板 CSS 契约测试；组件测试覆盖颜色选择、清空和关闭。完整自动化测试现为 138 项，本次未修改题目生成、判题、计时、存储、统计或会话结构。

## 分数与百分数转换改为填空输入（2026-08-03）

- 两个方向现在只使用 46 条固定公考百化分关系：21 个单位分数和 25 个常见非单位最简分数；删除接近基准随机分数、普通直除随机分数及旧三结构混合组卷。
- 分数转百分数题面统一为 `1/14 ≈ ___%`，用户输入的百分数会直接显示在题面答案位中，并且只按题库预设答案判定，不再使用允许误差范围。
- 百分数转分数题面统一为 `7.7% ≈` 后接标准上下分数输入位，分子与分母共用一条分数线；旧的手动切换提示已删除。每题首次在分子输入一个数字后自动切到分母，用户仍可手动点回分子修改，且手动返回后不会再次自动跳转。当前激活的输入位使用浅绿底和深绿内描边，并同步设置 `aria-pressed`，可明确区分正在修改分子还是分母。
- 百分数转分数只接受题库中对应的最简分数；`2/4` 等非最简等值形式、分母为 0 或格式不完整均判错。
- 题组按单位分数与非单位分数各 50% 覆盖；每个类别在自身固定题库用完前不重复，超过容量后才开始下一轮。统一生成器版本保持 `2.5.0`；该项未修改生成、判题、会话或历史数据结构，后续复盘工具栏测试使当前自动化测试增至 138 项。

## 移动端长题目明细布局（2026-08-03）

- 即时结算与历史详情继续共用 `QuestionDetails`；桌面端保留“题号 / 题目 / 作答 / 判定 / 正确结果 / 用时”六列表格。
- 在宽度不超过 480px 的手机页面中，题目改为独占第一行并允许完整换行，不再使用省略号截断；作答、判定、正确结果和用时带短标签排列在第二行。
- 该规则统一适用于分数比较、多位数相加、直除等所有题型，不增加题型特判，也不改变冻结题目、判题、计时、存储或历史数据。
- 自动化测试增至 132 项，新增明细响应式语义类和窄屏两层布局 CSS 契约断言。

## 复盘草稿、结算明细与除法题面修订（2026-08-03）

- 训练完成后的即时结算和历史详情现在共用“✎ 草稿”复盘入口；画布沿用训练时的撤销、清空和完成操作，仅存在于内存，不写入训练记录或历史。
- 题目明细删除旧版“重开”列，统一为“题号 / 题目 / 作答 / 判定 / 正确结果 / 用时”；判定只显示 `✓ / ×`，冻结正确结果单独展示。
- `quotient_estimate_3_percent` 的正确结果不显示接受范围，而显示真实商四舍五入保留两位小数；判题仍严格沿用相对误差不超过 3% 的既有规则。
- 三位数÷两位数生成器会跳过 `a % b === 0` 的候选，四种结构配额和商区间保持不变；统一生成器版本升至 `2.3.0`。
- 自动化测试当时增至 131 项，新增复盘草稿开关、明细列契约、3% 正确结果格式和非整除属性断言；后续移动端长题目布局测试使当前总数增至 132 项。

## 首页题型层级与三位数除法估算（2026-08-03）

- 首页现在显示 10 个一级训练入口；“分数转百分数”和“百分数转分数”分别作为独立卡片，但底层继续映射到 `fraction_percent_conversion` 的两个既有子模式，没有复制生成器或修改训练记录结构。
- “三位数÷两位数”继续作为一个一级题型，仅在被选中时显示紧凑的“商首位 / 商前两位 / 3%估算”答题要求；切换其他题型后整个子选项容器卸载，不保留空白。
- `quotient_estimate_3_percent` 沿用任务 1.7 的数字范围、结构配额和题组顺序；判题以真实商计算相对误差，`|输入－真实商| / |真实商| ≤ 3%` 时正确，并与其他两个除法规则分开保存和统计趋势。
- 新增一级入口映射、条件子选项、3%上下边界、全部合法题量配额、IndexedDB恢复和趋势隔离测试；这些入口测试继续保留在当前 138 项完整套件中。390×844 手机视口实测为两列一级网格、三段式子选项，无横向溢出，切换后正确隐藏且控制台无错误。
- 首页层级调整没有改变现有九类底层 `QuestionType`、会话字段、题量、计时、评级或其他题型；后续非整除修订仅改变三位数除法候选过滤并已单独记录。

## 训练答题区视觉统一（2026-08-03）

- 数值题的当前答案框在未输入时保持空白，不再显示横线占位；分数比较使用竖式题面中的动态关系符号。百分数转分数已在后续改为分子、分母双输入位。
- 数字键盘右下角“确定”文字由白色改为与“清空”相同的 `var(--ink)` 深绿色；按钮尺寸、禁用规则和提交逻辑未改变。
- 训练页左上角继续固定为“← 暂存”，未修改暂存、计时或恢复逻辑。
- 已更新页面交互和移动端 CSS 契约测试；本次未修改题目生成、判题、存储、统计或评级规则。

## 任务 1.6 修订：多位数相加（2026-08-03）

- 任务目标由混合“多数加减”收敛为固定的 4 个三位数连续相加；沿用内部题型标识 `multi_number_add_subtract` 以兼容旧历史，但用户界面显示为“多位数相加”。
- 结构按个位、十位、百位向高位的真实进位划分：单列进位 10%、双列进位 25%、三列连续进位 40%、高进位量 25%。高进位量要求至少一列进位值达到 2，正式题组不生成无进位题。
- 四档结构分别映射难度 2、3、4、5；每题冻结四个操作数、三列进位值、真实主结构和当前统一生成器版本 `2.5.0`，`secondaryTags` 继续为空。
- 所有操作数均为 100～999 的整数，符号固定为三个加号，答案为四数精确和；不再生成减法、三项相加、大数相加或加减混合题。
- 10～100 的全部合法整十题量继续使用公共最大余数算法分配精确配额；固定生成上下文可复现。旧记录保留原题面、原答案和原生成器版本，不按新结构回推。

## 任务 1.4：两位数×两位数结构化生成（2026-07-31）

- 两位数×两位数改为先分配唯一心算策略结构：接近整十占 30%、11～19 因子占 20%、个位较小便于拆分占 20%、普通题占 30%。
- 主结构按“接近整十 → 11～19 因子 → 个位较小 → 普通题”的固定优先级判定，避免一个题同时占多个配额；同时命中的客观策略会保存在 `secondaryTags`。
- 10～100 的全部合法整十题量均使用公共最大余数算法分配精确结构配额；生成器采用有限重试与确定性备用题，固定 `GenerationContext` 与确定性 ID 工厂时题组可复现。
- 任务 1.5 已修订：分数比较只保留直接判断、同向变化、接近二分之一和一般比较四种互斥主结构；新题不生成等值分数，比较真值仍由交叉相乘确定。
- 任务 1.6 已按 2026-08-03 修订规则完成：固定生成四个三位数相加，并按逐列进位负荷划分四种互斥结构。
- 任务 1.7 已完成：三位数÷两位数仅覆盖真实商区间 1～100，按首位误导、接近估商边界、商 1～10、商 10～100 生成；不再把商小于 1 误列为该题型结构。
- 任务 1.8 的五种商主结构已完成；复核确认三/四/五位除数 75%/15%/10% 仍只是声明目标，最终题组会因不可行组合进入备用题而失真，已拆出任务 1.8.1 修复。
- 任务 1.9 已完成并修订为固定百化分题库：两个方向共用同一关系表，只改变展示和严格判题方向。
- 自动化测试现为 138 项；任务 1.10 仅保留设计冻结，尚未实施。
- 已新增 GitHub Actions CI；每次推送和拉取请求都会自动执行只读格式检查、类型检查、Lint、138 项测试与生产构建。

## 移动端训练页单屏布局修复（2026-07-31）

- 训练页使用局部 `100dvh` / Flex 布局并禁止页面级滚动；首页、历史、详情和成绩页不受该滚动限制。
- 中间题目区允许收缩，标题、留白和装饰会随视口高度压缩；完整数字键盘固定保留在底部，最小触控高度为 44px。
- 数字键盘、分数比较和百分数转分数的专用作答区都避让 `safe-area-inset-bottom`。
- 分数比大小采用左右竖式分数和居中关系符号，底部只使用“大于/小于”选择按钮及“重开训练/确定”操作区；选中后关系符号与按钮同步高亮。
- 已在真实浏览器视口中验证 360×640、375×667、390×844、412×915；四种尺寸均无页面溢出，360×640 下完整键盘可见且首页仍可滚动。
- 新增四种视口的训练结构回归及 `100dvh`、局部滚动锁、安全区、最小按键高度 CSS 契约测试；未修改题目生成、判题、计时、存储或统计逻辑。

## 任务 1.3：两位数×一位数结构化生成（2026-07-31）

- 两位数×一位数改为按真实进位次数生成唯一主结构：无进位占 10%、一次进位占 40%、两次进位占 50%。
- 新题保持两位数被乘数、乘数 2～9、个位非 0；按个位和十位乘法产生的实际进位次数反向验证。乘数为 8 或 9 时保存客观 `high_multiplier` 辅助标签。
- 10～100 的全部合法整十题量均按精确配额生成，低价值整十被乘数不会进入正式题组；固定生成上下文可复现。
- 未修改加减法、两位数×两位数或其他题型、UI、题量规则和评分标准。任务 1.4 已在后续单独完成。

## 任务 1.2：三位数加减的进位/借位结构化生成（2026-07-31）

- 三位数加减改为按个位与十位的真实进借位次数生成唯一主结构：无进借位占 10%，一次进借位占 60%，两次连续进借位占 30%。
- 加法的百位不会产生千位进位；减法的百位始终足够借位，因此结果保持为非负三位数级整数。新题继续过滤个位为 0 的低价值数字。
- 新题保存原始 `a`、`b` 与 `add`，`primaryStructure` 可由逐位规则反向验证；`secondaryTags` 继续为空，不提前写入凑整、易混淆等主观标签。
- 10～100 的全部合法整十题量均使用公共最大余数算法分配精确结构配额；固定 `GenerationContext` 与确定性 ID 工厂时题组可复现。
- 未修改两位数加减、其他题型、UI、题量规则和评分标准。任务 1.2 完成时自动化测试为 94 项；后续任务 1.3、1.4 已单独完成。

## 任务 1.1：两位数加减的进位/借位结构化生成（2026-07-31）

- 两位数加减改为先分配唯一主结构、再直接构造符合条件的算式：无进借位占 10%，标准个位一次进/借位占 60%，边界挑战占 30%。
- 边界挑战的定义固定为加法跨百（`a + b >= 100`）或减法结果为个位数（`0 <= a - b <= 9`）；边界条件优先于进借位判定，因此主结构配额不重叠。
- 新题保存原始 `a`、`b` 与 `add`，`primaryStructure` 可由算式反向验证；本阶段保留 `secondaryTags` 接口但统一为空，不预设主观辅助标签。
- 10～100 的全部合法整十题量均使用公共最大余数算法分配精确结构配额；题组随机仅用于打乱顺序，不改变最终配额。固定 `GenerationContext` 与确定性 ID 工厂时可复现。
- 未修改三位数加减或其他题型、UI、题量规则和评分标准。任务 1.1 完成时自动化测试为 91 项；后续任务 1.2 已单独完成。

## 任务 0.2：题量选择扩展（2026-07-31）

- 首页题量改为应用内选择面板：快速模式固定 10 题、标准模式固定 20 题、自定义模式可选 10～100 题且步长为 10；首次进入自定义模式默认 30 题，并保留本次会话内最近一次合法自定义值。
- 新增唯一题量运行时契约，训练会话仅创建合法的整十题量；新会话持久化 `questionCount`，题量会随 active 暂存、恢复、结算和历史记录保持一致。
- 旧 10/20/50 题记录继续可读；旧记录缺少 `questionCount` 时按冻结题组长度读取；显式非法 active 题量会在存储边界被排除，不会导致页面崩溃。
- 评级继续按照实际题量线性缩放，未改变题型基础评级标准或正确率优先规则；题库配额工具已验证所有 10～100 的合法题量。
- 本任务当时未修改任何具体题型的数字生成、结构比例或评分标准；后续任务 1.1 已单独完成。该任务完成时自动化测试为 88 项。

## 任务 0.1：题库结构化公共契约（2026-07-31）

- 新增 `GENERATOR_VERSION`、`GenerationContext` 和确定性配额工具；生产环境继续使用 `Math.random` 与 `crypto.randomUUID`，测试可注入固定随机源和确定性 ID。
- 新生成题保存 `primaryStructure: "baseline_random"` 与空 `secondaryTags`，为后续结构化题库保留唯一主结构和可重叠辅助标签边界；本任务不改变任何题型的数字生成规则。
- 生成失败最多尝试 24 次，之后使用确定性备用题并通过可选回调记录回退事件；`nonRound` 同样有限抽取后使用确定性非整十值，不再依赖无限循环或递归。
- 旧 IndexedDB 题目缺少结构字段时规范化为 `legacy_unknown` 与空标签，不按旧数字反推新结构。
- 新增生成上下文复现、回退、整十随机源保护、10/20/50 题最大余数配额及旧题兼容测试；完整测试现为 60 项。

## 开发计划重审（2026-07-31）

- 已将后续工作按“训练数据可信 → 题库可信 → 页面与代码稳定 → 浏览器 E2E → 真机验证 → PWA → 上云设计 → 同步 → PK → 跨设备验收”重新排序。
- 该次计划重审时，训练数据可信阶段、任务 0.1、0.2 与任务 1.1～1.4 已完成；任务 1.5～1.9 后续也已实施。当前仍待修复的是多位数直除二级位数配额，以及专项训练、完整跨标签页互斥、浏览器 E2E、真机、PWA、Supabase、同步和 PK。
- 新计划把每种基础题库、跨标签页互斥、Playwright、真机、版本冻结、RLS、同步幂等和 PK 拆分为独立可验收任务；任务 0.1 已建立生成器版本、可注入随机源与 ID 工厂、主/辅助结构字段、有限重试/回退和旧题 `legacy_unknown` 兼容，下一项为两位数加减。

## 自动化测试补齐（2026-07-31）

- 新增 Active Session、整组重开训练和计时交互测试；覆盖继续、放弃、取消、后台暂停、恢复、旧状态舍弃和重复提交保护。
- 新增 HistoryList / HistoryCharts 组件测试；覆盖日期切换、用户分区、详情回调、1/10/100/1000 条趋势汇总与规则隔离。
- 使用 `fake-indexeddb` 直接测试真实存储接口：创建、更新、完成、放弃、重复 ID、旧记录兼容、重复 active 清理及读取失败。
- IndexedDB 读取失败会在首页显示可理解的错误提示；存储层不再静默吞掉异常。
- 修复恢复会话的渲染过渡中可能出现负用时：计时结果最低为 0。

验证结果：任务 0.2 完成时 `format`、`typecheck`、`lint`、`test`（88 项）和 `build` 均已通过；任务 1.1 后为 91 项，任务 1.2 后为 94 项，任务 1.4 后为 100 项，任务 1.5 后为 103 项，均已通过。

## 多标签页最低防护（2026-07-31）

- 点击开始训练前再次读取 IndexedDB；会话使用 `crypto.randomUUID()` 作为唯一 ID。
- 新增 `startInFlight`，忽略同一页面“开始练习”的快速重复点击。
- 已记录已知限制：两个标签页完全同时通过读取检查时，仍可能都进入训练；完整跨标签页锁将作为后续验收项实现。

## 代码质量修复（2026-07-31）

- 移除未使用的 `@supabase/supabase-js` 依赖，并同步更新 `package-lock.json`；当前不包含 Supabase 占位代码。
- `tsconfig.json` 已改为 `allowJs: false`，项目只接受 TypeScript/TSX 源文件并继续启用严格类型检查。
- active 会话的自动保存失败会显示应用内错误提示，不再留下未处理的 Promise 拒绝。
- 存储边界新增旧会话规范化：缺失的状态、暂停时长、重开次数、作答精度和草稿标记会补齐安全默认值；无法识别的原始记录不会进入业务页面。
- ESLint 已忽略 `.next` 构建产物、依赖目录、覆盖率目录和 Next 自动生成声明文件，`npm.cmd run lint` 可作为正式质量门槛。

> 任何功能、题库、数据结构、计时、判题或展示逻辑发生变化时，必须在同一次修改中更新本文件、`PROJECT_LOGIC_AUDIT.md` 和相关测试。

## 当前状态（2026-08-03）

项目：速算训练 V1 本地 MVP
技术：Next.js、React、TypeScript、IndexedDB、Canvas、Recharts、Vitest

### 已完成

- 首页选择用户、题型、子模式和题量。
- 9 类基础题生成与判题；百分数转分数使用分子/分母双输入位，两种转换都按固定百化分关系严格判定。
- 九类基础题型均已有真实主结构；多位数相加已固定为四个三位数并按逐列进位负荷精确配额。多位数直除的商主结构可信，但除数位数二级配额仍待任务 1.8.1 修复。
- 统一计算器键盘、比较键盘和 10% 可见度的临时手写草稿。
- 单题和整组有效计时；切后台、暂存时暂停；刷新时可继续或放弃未完成训练。
- 结算页、对齐的题目明细、复盘手写草稿、IndexedDB 历史记录。
- 历史记录可按日期切换，分别显示🐟与🐱当天训练，点击可查看完整作答详情。
- 成绩页按题型/答题规则拆分，🐟与🐱左右显示独立趋势与合格、良好、优秀目标。
- 趋势纳入全部历史；超过 20 条后自动按顺序汇总。每个点按实际题数加权计算，避免不同题量的训练被等权比较。
- 单一未完成训练约束：保存 active 会话时会在同一 IndexedDB 事务中清除旧 active 会话；开始新训练时可继续、放弃或取消暂存训练。
- “重开训练”会沿用当前会话冻结的用户、题型、答题规则和题量，直接生成全新的会话与题组；旧会话的已答记录、当前答案、草稿和计时状态均被舍弃，且快速重复点击不会创建多个替代会话。
- 已建立题目生成、计时、统计、键盘、存储、整组重开训练、历史日期切换及 active 会话继续/放弃/取消的自动化测试；旧版单题重开字段继续兼容读取。

### 重开训练交互修订（2026-08-03）

- 训练页答案框下方按钮由“重开本题”改为“重开训练”，点击后无需二次确认，直接开始一组全新的训练。
- 新训练复用当前会话冻结的用户、题型、子模式和题量，而不是重新读取首页选择；会话 ID、题组 ID、进度、答案、记录、草稿和计时全部重新开始。
- 新 active 会话通过现有 IndexedDB 单 active 事务替换旧会话；保存失败时不丢弃原会话，并显示应用内错误。
- 新增快速重复点击保护。旧记录中的 `restartCount` / `currentRestartCount` 字段继续兼容读取，但新流程始终从 0 开始且不再递增。

### 分数比大小界面优化（2026-08-03）

- 题面不再使用一行斜杠文本，改为左右两个带横线的竖式分数，中间显示 `?` 或当前选择的关系符号。
- 底部保留项目原有绿色体系，使用“大于/小于”两枚大触控按钮，并将“重开训练”和“确定”并列放在下一行。
- 未选择关系时“确定”禁用；恢复 active 会话时，训练页现在以会话冻结的题型和子模式选择专用作答控件，不再受首页当前选择影响。
- 新增组件交互和 CSS 布局契约测试；题目生成、答案、判题、计时、存储和统计规则均未改变。

### 分数比较符号判题修复（2026-08-03）

- 根因是生成器标准答案一直使用半角 `< / = / >`，而新版关系按钮错误写入全角 `＜ / ＝ / ＞`；严格字符串比较因此把相同方向判为错误。
- 新作答统一保存半角符号，判题边界同时兼容三种等价全角符号，避免未完成的旧 active 会话继续误判。
- IndexedDB 读取时仅修复受该缺陷影响的分数比较记录：把全角作答规范化为半角，并按题目冻结答案恢复 `isCorrect` 与 `accuracyLevel`；其他题型和记录不重新判题。
- 新增判题兼容和旧历史修复测试；题目数字、结构配额、计时和评级标准没有改变。

### 分数比较移除等值题（2026-08-03）

- 新题组删除 `equal_fractions` 主结构，配额改为直接判断 10%、同向变化 40%、接近二分之一 20%、一般比较 30%，生成器版本升至 `2.2.0`。
- 作答区删除“等于”，只保留“大于”和“小于”；生成器属性测试明确禁止新题答案为 `=`。
- 旧版已完成的等值题继续保留并可查看；含等值题的旧 active 题组不再恢复，避免用户进入没有可用答案按钮的训练。
- 本次没有改变非等值题的交叉相乘判定、计时、评级阈值或其他八类题库。

### 当前未完成

- 多位数直除的商结构/除数位数联合配额修复，以及全部合法题量的最终位数属性测试。
- 专项训练任务 1.10 仍只完成设计冻结，尚未实现入口、会话字段或专项统计。
- 当前关键训练、历史和详情组件测试，以及九类题型的主结构测试已完成；多位数直除的最终除数位数测试、完整浏览器级 E2E、真实移动端测试和 PWA 离线/更新测试尚未完成。
- Supabase 登录、云端同步、异步 PK 与权限规则。
- Service Worker 离线缓存、应用图标完善、Capacitor Android 封装。

## 本次变更（2026-07-31）

- 将 `DEVELOPMENT_PLAN.md` 扩展为执行版计划：为每个阶段明确问题、目标行为、涉及模块、数据规则、验收标准和暂不开展范围。
- 完成开发计划阶段 1：实现单一 active 会话约束、应用内继续/放弃提示以及旧版重开次数字段的持久化与详情兼容；接入 jsdom、React Testing Library 和 fake-indexeddb。2026-08-03 起训练页改为整组重开，不再产生新的单题重开次数。
- 新增长期趋势粒度测试，覆盖 10、100、1000 条记录，确认全部记录都参与汇总且趋势点数量受控。
- 修复历史日期选择：异步读取完成后默认选择最新日期；日期按钮可正常切换，训练按当天完成时间倒序显示。
- 趋势图从“最近十次”改为“全部历史”。记录数增大时自动变粗粒度：不超过 20 条逐条显示，21–100 条最多约 20 点，101–500 条最多约 25 点，超过 500 条最多约 30 点。
- 汇总趋势按题目数量加权平均耗时和正确率，提示信息展示每个点包含的训练组数。
- 新增统计测试，验证密集历史不会丢失记录且会受点数上限控制。

## 上次主要变更（2026-07-30）

- 曾修复百分数转分数四选一、暂存/恢复计时和草稿画板崩溃问题；其中四选一方案现已由分子、分母填空输入取代。
- 优化移动端布局、历史详情与双人趋势展示。
- 统一数值输入为带正负号和小数点的计算器键盘。

## 验证状态

最近一次验证（2026-08-03）：

- `npm.cmd run format`：通过
- `npm.cmd run format:check`：通过
- `npm.cmd run typecheck`：通过
- `npm.cmd run lint`：通过
- `npm.cmd run test`：138/138 通过
- `npm.cmd run build`：通过
