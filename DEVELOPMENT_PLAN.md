# 鱼猫速算训练营：后续开发计划

更新时间：2026-08-03
适用版本：V1 本地 MVP

> 本计划只安排尚未完成的工作。每个任务完成后，必须同步更新 `PROJECT_STATUS.md`、`PROJECT_LOGIC_AUDIT.md`、本文件和对应测试；未达到验收标准不得进入下一项。质量门槛固定为 `format:check`、`typecheck`、`lint`、`test`、`build` 全部通过；本地整理代码时可先运行会写入文件的 `format`。

## 阶段 0：当前已完成基线

### 已完成且已验证

- 本地单浏览器训练闭环：9 类基础题、数值/专用键盘、草稿、判题、结算、历史和趋势。
- 训练数据可信：单一 active 会话约束；继续、放弃、取消；整组重开训练原子替换；后台暂停和刷新恢复。旧版单题重开字段仅作历史兼容。
- 测试基线：Vitest 纯逻辑测试、React Testing Library 组件交互测试、`fake-indexeddb` 真实存储测试，共 138 项且最近一次通过。
- 最近一次完整本地验证已通过：`npm.cmd run format`、`npm.cmd run format:check`、`npm.cmd run typecheck`、`npm.cmd run lint`、`npm.cmd run test`、`npm.cmd run build`；GitHub Actions 已配置为在推送与拉取请求时自动执行只读质量门槛。

### 明确未完成

- 多位数直除的商结构/除数位数联合配额修复、完整题库难度模型；完整跨标签页互斥；浏览器 E2E；真机验证；PWA 正式离线能力。
- Supabase、登录、云同步、异步 PK、RLS 以及跨设备验收均未开始。
- 当前没有 Supabase 依赖、客户端或占位代码。

### 已有测试与后续测试边界

已覆盖：题目基础有效性与判题、计时、统计加权、键盘、active 会话、整组重开训练、历史组件和真实 IndexedDB。
尚未覆盖：专项训练实施、Playwright 浏览器 E2E、真实手机、PWA 离线/更新、Supabase 集成、RLS 与双设备 PK。九类基础题型的结构与配额属性均已有自动化测试。

### 任务 0.1：题库结构化公共契约

**状态：** 已完成（2026-07-31）。

**优先级：** 最高；这是阶段 1 的共同前置任务。

**前置条件：** 阶段 0 的现有质量门槛通过（已满足）。

**当前问题：** 已解决：此前 `generationRuleVersion` 固定为 `1.0.0`；各题型未共享结构分类、可复现随机源或失败降级规则。

**目标行为：** 已实现可复用的题库生成公共契约；后续每一类题只实现自己的结构规则和配额。

**涉及模块：** 题目类型、生成器公共工具、存储兼容边界、生成器测试和文档。

**数据与规则：** 每题保留唯一 `primaryStructure`（仅用于题组配额）和可重叠的 `secondaryTags`（仅用于分析）；`generatorVersion` 由唯一常量提供，并在每次改变生成语义时升级。生成器接收 `GenerationContext`：`random: () => number` 与 `createId: () => string`；生产环境分别使用 `Math.random` 和 `crypto.randomUUID`，测试环境同时注入固定种子随机源和确定性 ID 工厂。题组配额算法为：题量×比例后先取整数部分，剩余位置按小数余数从大到小分配，最终随机仅打乱顺序，不改变配额。每道题最多有限次尝试；失败使用确定性备用生成器，并保存真实回退结构标签；开发环境可记录回退次数。旧题缺少新结构字段时固定规范化为 `primaryStructure: "legacy_unknown"` 与 `secondaryTags: []`，不得按旧数字反推新标签；旧题可继续展示和参与既有题型趋势，但不进入新的结构表现统计。

**自动化测试：** 所有合法题量（10～100、步长 10）的配额严格等于题量；相同 `GenerationContext` 下题型、数字、主/辅助结构、答案、顺序和确定性 ID 可复现；生产 UUID 不参与固定种子比较；重试上限不会无限循环；回退题标签真实；旧记录保持其原题面、答案和旧生成器版本，缺字段统一为 `legacy_unknown` / 空标签且不会被新规则重解释。

**手动验收：** 生成不同题量的演示题组，核对主结构数量、顺序打乱和版本字段。

**验收标准：** 已达成：后续八类题库任务可引用同一生成上下文、版本、重试和标签契约；旧 completed 记录仍可打开且不被重生成、不被结构统计误归类。具体题型配额将在对应任务实施。

**本任务不做：** 不改变任何具体题型的数字范围、评分标准、UI 或完整的 `schemaVersion` / `gradingVersion` / `ratingVersion` 冻结。

### 任务 0.2：题量选择扩展为 10～100 的整十题量

**状态：** 已完成（2026-07-31）。

**优先级：** 高；题库结构化前的训练会话公共参数完善。

**前置条件：** 任务 0.1 已完成。

**当前问题：** 已解决：首页仅能选择 10 / 20 / 50，题量没有运行时统一校验，也未在会话中显式冻结选择值。

**目标行为：** 首页使用应用内面板提供快速（10）、标准（20）与自定义（10～100、步长 10）模式；自定义首次为 30，取消不修改已选题量。

**涉及模块：** `src/lib/question-count.ts`、首页、`TrainingSession`、IndexedDB 读取边界、题量/统计/配额测试和相关文档。

**数据与规则：** 合法题量唯一来源为 10～100 的整十数。新会话保存 `questionCount`，训练生成、暂存、恢复、结算和历史沿用冻结题组；旧记录缺少字段时按题组长度兼容，显式非法 active 记录不进入恢复流程。评级继续按实际题量线性缩放，基础阈值和正确率优先规则不变。

**自动化测试：** 已覆盖全部合法与非法输入、快速/标准/自定义选择、取消、最近自定义值、70 题 active 创建、10～100 配额总数和确定性、30/70/100 评级缩放、10/20/50 非回退以及非法 active 读取。

**手动验收：** 在移动宽度下打开面板，切换三种模式、拖动滑块、取消及确认后开始训练，确认按钮与滑块不被安全区遮挡。

**验收标准：** 已达成：新训练仅能创建合法题量；选择值能穿过本地训练完整流程；题库规则未改变。

**本任务不做：** 不修改任何具体题型的数字生成、结构比例、评分标准、Supabase、同步、PK 或 PWA。

---

## 阶段 1：题库结构化

### 任务 1.1：两位数加减的进位/借位结构化生成

**优先级：** 最高。

**前置条件：** 任务 0.1 完成。

**状态：** 已完成（2026-07-31）。

**当前问题：** 已解决：此前仅按范围随机生成，`进借位` 标签并不代表实际存在进位或借位。

**目标行为：** 已达成：两位数加减先选择唯一主结构，再直接构造符合结构的数字；`secondaryTags` 接口保留但暂不预设主观辅助标签。

**涉及模块：** `src/lib/generate.ts`、`src/lib/types.ts`、`src/lib/generate.test.ts`、相关文档。

**数据与规则：** 主结构配额为无进/借位 10%、标准个位一次进/借位 60%、边界挑战 30%。边界挑战的唯一、可验证定义为：加法结果跨百（`a + b >= 100`），或减法结果为个位数（`0 <= a - b <= 9`）；主结构按该挑战条件优先，再判断是否存在个位进/借位，避免重叠配额。减法仍默认非负；不新增小数题、不改变评分和题量规则。`secondaryTags` 接口保留，但本任务不预设“易混淆”等主观标签；先保存完整题目原始数据、运算方向和用户行为，后续有真实数据再决定辅助标签。

**自动化测试：** 已通过：结构检测函数；所有合法题量（10～100、步长 10）的精确配额；题目答案、主结构、原始数据与低价值数字过滤；固定生成上下文复现。

**手动验收：** 后续可在本地连续开始 20 题训练，抽查题面与记录标签一致。

**验收标准：** 已达成：每题主结构可由数字反推验证；配额按公共算法严格成立；旧训练记录可读取。辅助标签在本任务保持空数组，避免把主观判断写入历史。

**本任务不做：** 未处理三位数加减、UI 或其他题型。

### 任务 1.2：三位数加减的进位/借位结构化生成

**优先级：** 最高。

**前置条件：** 任务 1.1 完成。

**状态：** 已完成（2026-07-31）。

**当前问题：** 已解决：此前随机题未识别连续进/借位，标签不能反映真实训练结构。

**目标行为：** 已达成：三位数加减按唯一主结构直接构造；只统计个位和十位的真实进/借位次数，避免无关的千位进位影响分类。

**涉及模块：** `src/lib/generate.ts`、`src/lib/types.ts`、`src/lib/generate.test.ts`、相关文档。

**数据与规则：** 主结构为无进/借位 10%、一次进/借位 60%、两次连续进/借位 30%。加法百位不产生千位进位；减法百位足够借位，结果继续非负。配额与随机源复用任务 0.1 公共契约；`secondaryTags` 接口保留但本任务保持为空，接近整百和凑整不提前作为主观标签写入历史。

**自动化测试：** 已通过：三位逐位进/借位识别、连续结构、所有合法题量（10～100、步长 10）的配额、答案和原始数据一致性、固定生成上下文复现。

**手动验收：** 后续可抽查多组 20 题训练，确认连续进/借位题与主结构一致。

**验收标准：** 已达成：任何三位题均可确定归属唯一主结构；配额严格成立，辅助标签保持空数组且不影响统计。

**本任务不做：** 未改两位题生成、不增加负数或小数，也未实现专项训练。

### 任务 1.3：两位数×一位数结构化生成

**优先级：** 高。

**前置条件：** 任务 1.2 完成。

**状态：** 已完成（2026-07-31）。

**当前问题：** 已解决：此前只排除整十和乘 1，未保证进位次数、高乘数或训练价值。

**目标行为：** 已达成：生成器按无进位、一次进位、两次进位构造整数题；高乘数作为可验证辅助标签，低价值整十被乘数不进入题组。

**涉及模块：** `src/lib/generate.ts`、`src/lib/types.ts`、`src/lib/generate.test.ts`。

**数据与规则：** 主结构仅在无进位 10%、一次进位 40%、两次进位 50% 中分配；`高乘数` 是可重叠辅助标签，不参与配额。继续使用整数完整积。

**自动化测试：** 已通过：按位乘法进位识别；所有合法题量（10～100、步长 10）的精确配额；高乘数标签；低价值整十被乘数过滤；固定生成上下文复现。

**手动验收：** 后续可检查 20 题题组中各结构均符合标签和比例。

**验收标准：** 已达成：结构标签由真实算式验证，题组比例测试可重复通过。

**本任务不做：** 未改变两位数×两位数规则，不引入近似乘法或小数。

### 任务 1.4：两位数×两位数结构化生成

**优先级：** 高。

**前置条件：** 任务 1.3 完成。

**状态：** 已完成（2026-07-31）。

**当前问题：** 已解决：此前均匀随机，未落实接近整十、11–19、便于拆分和普通题的既定分布。

**目标行为：** 已达成：先选择互斥的乘法策略结构，再生成可验证的精确整数题。

**涉及模块：** `src/lib/generate.ts`、`src/lib/types.ts`、`src/lib/generate.test.ts`。

**数据与规则：** 接近整十 30%、11–19 20%、个位较小便于拆分 20%、普通题 30% 仅指唯一主结构；同时命中的特征写入辅助标签。配额、随机源与重试复用任务 0.1。

**自动化测试：** 已通过：每类结构识别、所有合法题量（10～100、步长 10）的配额、完整答案正确性、固定优先级、有限重试回退和固定生成上下文复现。

**手动验收：** 后续可查看多组题，确认题面能使用对应拆分技巧而非纯随机。

**验收标准：** 已达成：题目、标签、比例和答案均可由测试证明。

**本任务不做：** 未做交叉相乘专项，不改键盘或评分目标。

### 任务 1.5：分数比较结构化生成（已完成）

**优先级：** 高。

**前置条件：** 任务 1.4 完成。

**当前问题：** 旧版本曾包含等值题，但产品交互现只训练大小方向，不再需要“等于”答案。

**目标行为：** 按策略生成直接判断、同向变化、接近基准分数和一般比较，且新题组中的左右分数必须不相等。

**涉及模块：** `src/lib/generate.ts`、`src/lib/types.ts`、`src/lib/generate.test.ts`。

**数据与规则：** 直接判断 10%、同向变化 40%、接近二分之一 20%、一般比较 30%；不再包含 `equal_fractions`。真值仍以整数交叉相乘计算，生成器版本为 `2.2.0`。

**自动化测试：** 已通过：四种结构的识别、所有合法题量（10～100、步长 10）的配额、比较真值、禁止等值题、分数合法性和固定上下文复现。

**手动验收：** 逐题核对题面策略和 `< / >` 标准答案，作答区只显示“大于/小于”。

**验收标准：** 训练组不会被同分母等低难度题主导，所有结构有稳定标签和测试。

**本任务不做：** 不引入资料分析材料、排序题或 UI 教学提示。

### 任务 1.6：多位数相加（已按修订规则完成）

**优先级：** 中高。

**前置条件：** 任务 1.5 完成。

**状态：** 已按修订规则完成（2026-08-03）。

**当前问题：** 旧实现把三项/四项、三位/大数、加法/减法混在同一入口中，各结构耗时不可直接比较，也不符合本题型专门训练多项竖式加法的目标。

**目标行为：** 每题固定为 4 个三位数连续相加，用逐列进位次数和进位量形成稳定、可比较的难度梯度。

**涉及模块：** `src/lib/generate.ts`、`src/lib/types.ts`、`src/lib/generate.test.ts`。

**数据与规则：** `single_column_carry` 10%（仅一列进位且进位值为 1，难度 2）；`double_column_carry` 25%（两列进位且进位值均为 1，难度 3）；`triple_column_carry` 40%（个位、十位、百位均进位且进位值为 1，难度 4）；`high_carry_load` 25%（三列均进位且至少一列进位值达到 2，难度 5）。正式题组不生成无进位题。每题保存四个三位数、三个加号、`onesCarry`、`tensCarry`、`hundredsCarry`、真实主结构及生成器版本；旧题不按新规则回推。

**自动化测试：** 已覆盖四档结构识别、所有合法题量（10～100、步长 10）的精确配额、4 个三位数约束、固定加号序列、逐列进位元数据、精确答案、生成器版本和固定上下文复现。

**手动验收：** 抽查 10、50、100 题题组，题面始终为四个三位数相加；简单题不形成主体，连续进位与高进位量题占多数。

**验收标准：** 四档结构可由题面反算且配额稳定；不出现减法、三项、大数混合、无进位或错误难度标签。

**本任务不做：** 不做减法、三项相加、四/五位数混合、括号、多步公式、材料题或小数题。

### 任务 1.7：三位数÷两位数结构化生成与有效位说明（已完成）

**优先级：** 中高。

**前置条件：** 任务 1.6 完成。

**当前问题：** 已解决：旧计划曾错误包含商小于 1；按当前三位被除数和两位除数范围，真实商必然大于 1。现有题组已控制首位误导、边界估商和两档商数量级。

**目标行为：** 已达成：按商数量级和估商难点生成；题面按子模式明确“取有效数字、不四舍五入”或“3%估算”。

**涉及模块：** `src/lib/generate.ts`、`src/lib/types.ts`、训练/结算展示组件、`src/lib/generate.test.ts`、组件测试。

**数据与规则：** 被除数 103～997、除数 11～99，二者个位均非 0，且拒绝整除题。主结构为商 1～10（25%）、商 10～100（35%）、首位误导（20%）、接近整数估商边界（20%）；分类优先级为首位误导、边界、数量级。子模式为 `quotient_first`、`quotient_two` 和后续维护新增的 `quotient_estimate_3_percent`。前两种截取有效数字且不四舍五入；3% 模式以真实商的相对误差判定。该题型不包含商小于 1。

**自动化测试：** 已覆盖两档商数量级、首位误导、边界结构、所有合法题量（10～100、步长 10）的精确配额、非整除约束、3% 接受边界和趋势分轨。

**手动验收：** 分别完成商首位、商前两位和 3% 估算训练，确认提示、结果详情和判题一致。

**验收标准：** 已达成：用户无需猜测输入格式；四种互斥结构与非整除约束可由冻结数字反向验证。

**本任务不做：** 不实现精确长除法或资料分析选项题；3% 模式已由后续独立维护项完成。

### 任务 1.8：多位数直除结构化生成（主结构已完成，二级位数配额待修复）

**优先级：** 中高。

**前置条件：** 任务 1.7 完成。

**当前问题：** 五种商主结构已完成并能保持精确配额；但商结构列表与除数位数列表按固定索引配对，部分组合在数学上不可能，失败后的备用题会改变除数位数，因此最终题组不保证声明的 75%/15%/10%。

**目标行为：** 保留五种可验证商结构，同时让最终题面的三/四/五位除数真实满足目标配额；不能以“请求过某个位数”代替验证最终题面。

**涉及模块：** `src/lib/generate.ts`、`src/lib/types.ts`、`src/lib/generate.test.ts`。

**数据与规则：** 当前商结构配额为小于 1（15%）、1～10（25%）、10～100（30%）、100 以上（15%）、接近整数边界（15%），分类时边界优先。除数位数目标仍为三位 75%、四位 15%、五位 10%，但必须改成可行组合的联合分配；随机题继续排除个位 0。

**自动化测试：** 当前仅验证 100 题时五种商结构配额和结构反算。待补：所有合法题量的商结构配额、最终除数位数配额、每种联合组合可行性、有效位答案、固定上下文复现、有限回退与最终标签一致性。

**手动验收：** 抽查题组，确认不被 `80000÷400` 等漂亮数字主导。

**验收标准：** 尚未完全达成；必须以最终生成题目的实际除数位数和真实商结构同时通过属性测试，才能重新标记完成。

**本任务不做：** 不修改趋势加权规则，不新增小数直除。

### 任务 1.8.1：修复多位数直除联合配额

**优先级：** 最高；专项训练前置修复。

**前置条件：** 任务 1.8 的现状审查已完成。

**当前问题：** 两个独立配额数组按索引硬配对，产生“五位被除数÷三位除数但要求商小于 10”等不可能组合；备用题虽能恢复商结构，却破坏除数位数配额。

**目标行为：** 以数学可行性为约束联合分配商结构和除数位数；完成题组同时满足两项最终配额，且任何备用题仍保持被分配的联合结构。

**涉及模块：** `src/lib/generate.ts`、`src/lib/generate.test.ts`、生成器版本与四份项目文档。

**数据与规则：** 不改变五种商结构比例、三/四/五位除数目标比例、有效位答案或整数题范围。联合配额必须对 10～100 的合法题量有确定性的取整规则；若某个精确二维配额在小题量下不可行，需先明确稳定的优先级和误差上限，不能静默回退。

**自动化测试：** 覆盖所有合法题量的最终商结构与最终除数位数；逐题反算结构、位数和答案；固定生成上下文可复现；极端随机源在有限次数内完成且备用题不改变联合标签。

**手动验收：** 抽查 10、20、50、100 题，确认商范围与除数位数分布都来自最终题面，且无漂亮整十题主导。

**验收标准：** 所有属性测试通过，文档不再使用“约”掩盖配额失真，任务 1.8 才可恢复为完全完成。

**本任务不做：** 不新增小数直除、精确长除法、选项题或专项训练页面。

### 任务 1.9：分数—百分数转换题库补强（已完成）

**优先级：** 中高。

**前置条件：** 任务 1.8 完成。

**当前问题：** 候选池、标准/普通分数比例、接近基准和直除题均未受控；两个转换方向缺少各自的结构配额和边界测试。

**目标行为：** 建立可配置的常用分数池和普通分数策略；两个方向均使用题面填空输入。

**涉及模块：** `src/lib/generate.ts`、`src/lib/types.ts`、`src/lib/generate.test.ts`、评分测试。

**数据与规则：** 两个子模式共用一张冻结的高价值百化分关系表，完整包含 21 个指定单位分数和 25 个指定常见非单位最简分数。删除接近基准随机分数、普通直除随机分数和旧三结构混合配额。题组以 `unit_fraction` / `common_non_unit_fraction` 各 50% 覆盖；每个类别在自己的题库耗尽前不重复，超过容量才开启下一轮。`fraction_to_percent` 显示 `分数 ≈ ___%` 并严格匹配预设百分数；`percent_to_fraction` 显示 `百分数 ≈ __ / __` 并只接受冻结的最简分数。

**自动化测试：** 逐项锁定 46 条固定关系；两个方向验证全部合法题量的单位/非单位精确覆盖、类别内无提前重复和固定上下文复现；分数转百分数验证只接受预设值；百分数转分数验证最简分数接受、非最简等值形式、零分母和错误值拒绝。

**手动验收：** 检查两个方向的答案位都嵌入题面，百分数转分数可清楚切换分子和分母，手机单屏键盘完整可用。

**验收标准：** 两个子模式只出现固定百化分关系，题组覆盖和去重符合规则，严格判题无模糊接受。

**本任务不做：** 不做分数教学、资料分析公式题或新增小数基础题。

### 任务 1.10：专项训练

**优先级：** 中高。

**前置条件：** 任务 1.1～1.9 与纠偏任务 1.8.1 全部完成并通过各自的结构、配额与回归测试。

**当前问题：** 当前训练只按题型和子模式创建综合题组，无法区分用户是在综合训练中遇到某个结构，还是主动针对该结构练习；若直接修改 `primaryStructure`，会把“题目本身是什么”与“用户为什么选择这组题”混为一谈。

**目标行为：** 在既有题库结构稳定后，提供可配置的专项训练预设。专项训练只筛选或组合已存在的真实 `primaryStructure`，不重写题目结构定义；历史与统计可区分综合训练和主动专项训练。

**涉及模块：** 后续的训练会话类型、专项预设配置、题组创建服务、首页入口、训练/结算/历史筛选与统计、对应测试及文档。本任务当前只冻结设计，不实施这些模块。

**数据与规则：** 后续新增 `type TrainingMode = "comprehensive" | "specialized"`；新增可选 `SpecializedTrainingConfig`：`presetId`、`questionType`、可选 `operation: "addition" | "subtraction"`、`allowedPrimaryStructures: string[]`。新会话在 `trainingMode: "specialized"` 时必须保存该冻结配置；综合训练使用 `trainingMode: "comprehensive"`，不携带专项配置。题目仍按既有格式保存真实 `primaryStructure` 与 `secondaryTags`，专项配置绝不改写题目标签。旧会话读取时默认视为 `comprehensive`，不得根据旧数字回推专项来源。首批预设只能引用现有真实结构：加减法使用两位数 `single_carry_or_borrow` / `boundary_challenge`、三位数 `double_carry_or_borrow`、四数相加 `triple_column_carry` / `high_carry_load`；乘法使用两位数×一位数 `double_carry`（高乘数需另结合客观 `high_multiplier` 标签）、两位数×两位数 `near_ten`；直除使用三位数除法 `leading_digit_mislead` / `near_estimate_boundary`，以及多位数直除在 1.8.1 修复后的 `quotient_less_than_one` / `near_estimate_boundary`；分数使用分数比较 `same_direction` / `near_half` / `general_comparison`，百化分使用 `unit_fraction` / `common_non_unit_fraction`。不再规划不存在的“两位数加法两次进位”主结构，也不再规划已从百化分题库删除的“普通分数直除”。

**自动化测试：** 本轮不新增测试。实施时单独验证预设合法性、训练模式与结构筛选、旧会话默认值、综合/专项统计隔离、冻结配置恢复、错误预设拒绝和历史详情展示。

**手动验收：** 本轮无页面或行为变更。实施时分别完成同一结构的综合训练与专项训练，确认题组结构相同但会话来源、历史筛选和统计口径可区分。

**验收标准：** 本轮验收为数据模型、范围和边界已冻结：专项训练不是新的 `primaryStructure`，不修改既有题目标签；实施完成后，专项会话可从冻结配置复现其选择原因，且与综合会话独立统计。

**本任务不做：** 本轮不增加页面、不修改 `TrainingSession` 或其他会话结构、不写生成器代码、不新增专项训练测试，也不提前实现专项训练功能。

---

## 阶段 2：本地工程稳定

### 已完成：移动端训练页单屏布局修复（2026-07-31）

训练视图已改为局部 `100dvh` Flex 布局，中间内容可收缩，完整键盘固定保留在底部并避让安全区；滚动锁没有施加到 `body`，首页、历史和详情页继续正常滚动。已增加 360×640、375×667、390×844、412×915 四种视口的组件结构回归与 CSS 契约测试，并完成真实浏览器尺寸测量。本修复未改变题目生成、判题、计时、存储或统计逻辑。

### 已完成：训练答题区视觉统一（2026-08-03）

所有题型共用的答案框已移除空值横线占位；数字键盘“确定”使用与“清空”一致的深绿色文字，左上角继续保留暂存入口。页面交互测试覆盖空答案框，CSS 契约测试覆盖确定键文字颜色。本项没有改变答题值、提交条件、计时、暂存或恢复逻辑。

### 已完成维护：重开训练替代重开本题（2026-08-03）

训练页答案框下方统一显示“重开训练”。点击后沿用当前会话冻结的用户、题型、子模式和题量，创建新会话与新题组；旧进度、记录、输入、草稿和计时直接舍弃。IndexedDB 使用单 active 事务替换，写入失败则保留原训练并显示应用内错误；按钮具有快速重复点击保护。自动化测试验证新旧会话/题组 ID 不同、状态归零、冻结参数不变及旧版重开字段兼容。本项不修改题目生成规则、判题、评级、历史趋势或云端计划。

### 已完成维护：分数比大小移动端界面（2026-08-03）

分数比较题面改为左右竖式分数和中间动态关系符号，专用作答区现为“大于/小于”大按钮与并列的“重开训练/确定”操作按钮，继续使用项目原有绿色配色和安全区规则。训练页所有题型分支改为读取 active 会话冻结的 `questionType` / `subtype`，修复恢复会话后可能显示错误键盘的问题。组件测试覆盖竖式数字、关系选择、禁用提交与重开入口，CSS 契约测试覆盖分数线及两层操作区。本项不修改题目生成、判题、计时、存储或统计规则。

### 已完成维护：分数比较全角符号误判修复（2026-08-03）

分数比较按钮改为写入与生成器标准答案一致的半角 `< / = / >`。判题层对旧版全角 `＜ / ＝ / ＞` 做等价规范化；存储读取边界只针对分数比较记录修复受影响的 `userAnswer`、`isCorrect` 和 `accuracyLevel`，并同步规范化旧 active 会话的当前答案。其他题型的冻结判题结果保持不变。页面交互测试现会提交正确关系并验证结算为 1/1，另新增全角判题兼容与 fake IndexedDB 历史修复测试。

### 已完成维护：分数比较移除等值题（2026-08-03）

新题库删除 `equal_fractions`，四种剩余主结构按 10% / 40% / 20% / 30% 精确配额，生成器版本升至 `2.2.0`；界面删除“等于”按钮，只保留“大于/小于”。旧 completed 等值题仍按冻结数据展示，含等值题的旧 active 会话不再恢复，以免形成无法作答的题组。属性测试覆盖全部合法题量且断言答案永不为 `=`，组件和 CSS 契约测试覆盖双按钮布局，fake IndexedDB 测试覆盖旧 active/completed 分流。

### 已完成维护：首页题型层级与三位数除法 3% 估算（2026-08-03）

首页选择器已拆为独立组件并提供 10 个一级入口；分数转百分数与百分数转分数分别映射到底层 `fraction_percent_conversion` 的两个既有子模式。三位数÷两位数仍是一张一级卡片，选中后才挂载紧凑的三项答题要求，切换题型立即卸载。新增 `quotient_estimate_3_percent` 仅扩展必要的状态白名单、规则标签和判题：沿用任务 1.7 的题目数字、结构配额与顺序，以真实商为基准接受相对误差不超过 3% 的输入，并在存储和趋势中独立分轨。自动化测试覆盖 10 个入口映射、条件显示、移动端触控尺寸、所有合法题量配额、3% 边界、IndexedDB 恢复和趋势隔离；本项没有改动训练记录结构、题量、计时、评级或其他题库规则。

### 已完成维护：复盘草稿、明细分列与三位数除法非整除约束（2026-08-03）

即时结算和历史详情通过共用的 `QuestionDetails` 提供临时手写复盘画布，笔迹不保存。题目明细移除旧“重开”列，将判定与正确结果拆开；3% 估算题的正确结果显示真实商四舍五入后的两位小数。三位数÷两位数生成器从 `2.3.0` 起排除整除候选，但保持任务 1.7 的数字范围、四种主结构和配额不变。组件测试覆盖草稿开关、六列明细和正确结果格式，生成器属性测试覆盖全部合法题量中的非整除约束。本项不改变判题阈值、会话结构、计时、评级或趋势加权。

### 已完成维护：移动端长题目明细两层布局（2026-08-03）

即时结算和历史详情继续共用同一明细组件。桌面端保留六列表格；宽度不超过 480px 时，完整题面独占第一行并自动换行，作答、判定、正确结果和用时在第二行显示各自标签。该维护统一覆盖所有题型，不改变题目生成、冻结数据、判题、计时、存储、历史或统计规则；组件与 CSS 契约测试验证了稳定语义类和窄屏布局。

### 已完成维护：复盘草稿悬浮入口与三图标工具栏（2026-08-03）

即时结算和历史详情的草稿入口改为避让安全区的悬浮按钮，长明细滚动期间始终可点击。草稿画布底栏统一为颜色、清空、关闭三个图标按钮；颜色面板提供深绿、黑、红、蓝，颜色随每条笔画冻结，切换时不改变既有笔迹。组件测试覆盖颜色面板、清空和关闭，CSS 契约测试覆盖悬浮入口、安全区及三列图标工具栏。本项不保存草稿，不修改题目、判题、计时、存储、统计或会话结构。

### 已完成维护：分数与百分数转换题面填空（2026-08-03）

两个转换方向的答案位均嵌入题面。分数转百分数通过统一数字键盘输入百分数；百分数转分数删除四选一和 `options` 数据，改为可切换的上下分子、分母输入位，并以单个 `分子/分母` 字符串沿用现有会话结构。后续维护已将两条独立下划线收敛为一条标准分数线，删除手动切换提示，并实现每题首次分子数字输入后自动切到分母；用户手动返回分子时，本题不再自动跳转。当前输入位以浅绿底、深绿内描边和 `aria-pressed` 明确反馈选择状态。该交互最初发布于生成器 `2.4.0`，后续 `2.5.0` 已将生成与判题收敛为固定百化分题库和最简分数严格判定。本项不改变题量、计时、存储或趋势分轨。

### 任务 2.1：清理旧渲染分支

**优先级：** 中。

**前置条件：** 阶段 1 全部完成并验证。

**当前问题：** `page.tsx` 仍有被 `false` 禁用的旧结果/历史渲染分支，和新组件并存，增加维护歧义。

**目标行为：** 删除确认不再使用的旧分支，保留现有组件渲染行为。

**涉及模块：** `src/app/page.tsx`、相关组件测试、审查文档。

**数据与规则：** 不改变训练、历史、计时、评分、题目生成或存储格式。

**自动化测试：** 运行现有页面、历史和详情测试；如旧分支有专属断言，迁移到现有组件。

**手动验收：** 首页、训练、结算、历史、详情和成绩页均可正常进入。

**验收标准：** 无死分支；现有行为与当前 100 项基线不回退。

**本任务不做：** 不进行大规模页面重构。

### 任务 2.2：拆分过大的页面组件

**优先级：** 中。

**前置条件：** 任务 2.1 完成。

**当前问题：** `src/app/page.tsx` 同时承载首页、训练、结算、历史、成绩、会话恢复和存储编排，后续改动风险高。

**目标行为：** 将视图组件和页面级流程编排拆分为职责明确的模块，同时保留单一存储边界。

**涉及模块：** `src/app/page.tsx`、新/现有展示组件、相关测试。

**数据与规则：** 状态字段、存储接口、题库、评分和趋势规则不变；不在组件内散落 IndexedDB 细节。

**自动化测试：** 现有 active、重开、历史、错误提示交互全部保留；为拆分出的关键边界补最小回归测试。

**手动验收：** 完成一组训练、刷新恢复、查看历史和趋势。

**验收标准：** 页面职责可读，测试仍通过，无行为性改动。

**本任务不做：** 不改 UI 视觉设计，不新增页面能力。

### 任务 2.3：完整跨标签页互斥

**优先级：** 高。

**前置条件：** 任务 2.2 完成。

**当前问题：** 当前只有开始前二次读取和单页防连点；两个标签可同时读取到无 active 并同时进入训练。

**目标行为：** 同源两个标签同时开始时，最多一个获得训练权；另一个显示应用内提示，并能查看历史。

**涉及模块：** 页面会话编排、`src/lib/storage.ts` 或独立协调模块、相关组件和测试。

**数据与规则：** 优先评估 Web Locks；兼容方案为 BroadcastChannel + IndexedDB 复核。锁须与 session ID 关联，放弃、完成、页面关闭和异常恢复均应释放或过期；不能使用浏览器原生弹窗。

**自动化测试：** 双上下文或可测试协调层：同时开始、重复消息、锁持有、放弃释放、完成释放、失效锁恢复。

**手动验收：** 两个同源标签同时点击开始；关闭或放弃持锁页后另一页重新开始。

**验收标准：** 不会出现两个可提交的本地 active 训练；提示可理解；已知限制从审查文档移除或降级为浏览器兼容性说明。

**本任务不做：** 不做跨设备锁，不接入云端。

### 任务 2.4：Playwright 浏览器 E2E

**优先级：** 高。

**前置条件：** 任务 2.3 完成。

**当前问题：** 现有 100 项测试没有真实浏览器导航、刷新、IndexedDB 生命周期和多标签页行为。

**目标行为：** 建立可重复的本地浏览器 E2E 回归流程。

**涉及模块：** Playwright 配置、测试夹具、开发脚本和文档；不改变业务逻辑。

**数据与规则：** 流程覆盖：选择用户/题型 → 开始 → 输入 → 暂存 → 刷新 → 恢复 → 完成 → 结算 → 历史详情。另覆盖后台切换、整组重开训练、重复提交、active 恢复、日期切换、跨标签互斥和存储错误提示。

**自动化测试：** 上述 Playwright 用例；测试数据隔离且不会污染真实本地数据。

**手动验收：** 本地命令可一键运行，失败时有可定位截图或报告。

**验收标准：** 关键路径在真实 Chromium 中稳定通过；单元/组件测试继续保留。

**本任务不做：** 不把 E2E 替代真机测试，不测试 Supabase。

### 任务 2.5：部署预览环境与环境变量分层

**优先级：** 高。

**前置条件：** 任务 2.4 完成。

**当前问题：** Service Worker、PWA 安装和跨手机测试需要 HTTPS；未来 Supabase 迁移与 RLS 测试不能直接在正式环境进行。

**目标行为：** 建立 `local`、`preview/test`、`production` 三层环境和明确的发布路径。

**涉及模块：** Git 分支约定、Vercel（或同等 HTTPS 托管）环境、环境变量文档、测试 Supabase/正式 Supabase 项目规划。

**数据与规则：** 本地不提交密钥；preview/test 不连接正式数据；破坏性迁移和 RLS 测试只允许在隔离测试项目；生产发布必须经过预览环境验证。

**自动化测试：** 部署构建 smoke test；环境变量缺失时应用不暴露密钥且有明确失败行为。

**手动验收：** 从预览 HTTPS 地址完成一次本地训练、历史读取和浏览器 E2E smoke test。

**验收标准：** 三个环境的用途、数据隔离、密钥位置和发布入口均被记录且可执行。

**本任务不做：** 不接入 Supabase 客户端，不实施云同步或 PK。

---

## 阶段 3：手机与 PWA

### 任务 3.1：真机测试

**优先级：** 高。

**前置条件：** 阶段 2 E2E 通过。

**当前问题：** 目前只有桌面/模拟环境证据，移动端的前后台、触控和安全区未验证。

**目标行为：** 在真实手机收集可复现的兼容性结论和问题清单。

**涉及模块：** 无代码修改；测试记录和文档。

**数据与规则：** 测试安卓 Chrome、微信内置浏览器；前后台、锁屏、横竖屏、安全区、草稿触控、10/50/100 题和长历史性能。

**自动化测试：** 无；这是手动真机测试，不将浏览器 E2E 误写为替代品。

**手动验收：** 形成设备、系统、浏览器、步骤、结果和截图的测试表。

**验收标准：** 每项标记通过、阻塞或待修复，阻塞项进入任务 3.2。

**本任务不做：** 不顺手修复问题，不做 PWA 缓存。

### 任务 3.2：真机问题分诊与独立修复任务创建

**优先级：** 高。

**前置条件：** 任务 3.1 已形成问题清单。

**当前问题：** 真机发现的问题若被概括为“统一修复”，会违背一次只安排一个可独立验收任务的规则。

**目标行为：** 将每个已复现问题登记为独立修复任务，并只启动其中优先级最高的一项。

**涉及模块：** 问题登记、开发计划、状态文档；具体代码模块在后续独立修复任务中确定。

**数据与规则：** 每项登记必须包含设备/浏览器、复现步骤、预期/实际、影响等级、拟改模块和回归范围；不得将多个无关问题打包修复。

**自动化测试：** 每个后续修复任务自行决定可自动化的组件/E2E回归；不能自动化的必须保留真机复测步骤。

**手动验收：** 形成可排序的独立问题清单，并为首项问题建立单独验收条目。

**验收标准：** 不存在模糊的“批量修复真机问题”任务；每个修复均能单独回退、验证和更新状态。

**本任务不做：** 不直接修改业务代码，不在没有真机证据时进行泛化“移动端优化”。

### 任务 3.3：manifest 与图标

**优先级：** 中。

**前置条件：** 任务 3.2 的阻塞问题关闭。

**当前问题：** 尚未完成可安装 PWA 的应用标识资源。

**目标行为：** 提供正确名称、主题色、图标和 manifest 元数据。

**涉及模块：** `public/`、Next metadata/manifest、文档。

**数据与规则：** 图标尺寸、掩码图标和名称符合浏览器安装要求；不改变离线策略。

**自动化测试：** 构建时验证 manifest 可访问；可增加静态资源存在性检查。

**手动验收：** 安卓 Chrome 显示正确安装名称和图标。

**验收标准：** manifest 可用且不影响现有页面。

**本任务不做：** 不添加 Service Worker 或离线缓存。

### 任务 3.4：Service Worker 和离线缓存

**优先级：** 中。

**前置条件：** 任务 3.3 完成。

**当前问题：** 当前没有正式离线资源策略，刷新可能依赖网络。

**目标行为：** 明确缓存应用壳和本地训练所需静态资源；离线时新训练与本地历史可用。

**涉及模块：** Service Worker、缓存配置、构建配置、文档。

**数据与规则：** 不缓存用户云端数据；缓存版本可更新和失效；IndexedDB 仍是训练数据唯一来源。

**自动化测试：** 浏览器离线场景测试应用壳、训练启动和历史读取。

**手动验收：** 安装后断网打开、训练、刷新、恢复网络。

**验收标准：** 声明的离线功能可用，未声明的功能有可理解提示。

**本任务不做：** 不实现同步队列或 PK。

### 任务 3.5：PWA 安装与更新测试

**优先级：** 中。

**前置条件：** 任务 3.4 完成。

**当前问题：** 缓存更新可能导致旧资源或旧数据解释不一致。

**目标行为：** 验证安装、更新、重启与旧 IndexedDB 数据共存。

**涉及模块：** PWA 更新策略、测试记录、文档。

**数据与规则：** 更新不能删除 completed 历史；旧会话仍经兼容边界读取。

**自动化测试：** 缓存版本/更新策略的浏览器测试；旧数据夹具回归。

**手动验收：** 安装旧版生成历史，升级后检查恢复、历史和训练。

**验收标准：** 更新可感知、可恢复，训练数据不丢失。

**本任务不做：** 不封装 Capacitor，不接入云端。

---

## 阶段 4：上云前设计冻结

### 任务 4.0：身份与配对方案产品决策

**优先级：** 最高；阶段 4 的启动门槛。

**前置条件：** 阶段 3 完成，且已有 preview/test 环境规划。

**当前问题：** 本地 fish/cat 仅是前端训练身份；它不能安全证明谁能修改成绩、接受挑战或读取 PK。真实账号与“首页只显示🐟或🐱”的体验尚未作出明确决策。

**目标行为：** 在任何 Supabase 表、RLS 或客户端实现前，确定认证和配对方式。

**涉及模块：** 产品决策记录、Auth 方案、身份映射、隐私说明。

**数据与规则：** 推荐两个真实账号首次完成登录或配对码绑定，后续自动保持登录；首页仍可只显示当前🐟/🐱身份。若选择无认证方案，必须明确放弃可靠 RLS 和跨设备 PK 的范围，不能假装前端字符串具备权限能力。

**自动化测试：** 为选定方案定义登录、恢复、失效、登出、配对和账号切换的集成测试案例。

**手动验收：** 评审两台设备、换机、账号丢失和误选身份的体验与数据风险。

**验收标准：** 形成唯一决策；后续 Auth、RLS、同步和 PK 均引用该决策，不再混用本地身份与认证身份。

**本任务不做：** 不安装 Auth SDK，不写登录页面。

### 任务 4.1：数据版本与规则版本冻结

**优先级：** 最高。

**前置条件：** 阶段 3 完成。

**当前问题：** 题目仅有 `generationRuleVersion`；会话、判题、评级和云端迁移尚无统一冻结协议。

**目标行为：** 定义并迁移 `schemaVersion`、`generatorVersion`、`gradingVersion`、`ratingVersion`，保证已完成记录不可被新规则重新解释。

**涉及模块：** 类型、存储规范化、版本常量、迁移设计、文档和测试。

**数据与规则：** 新字段必须兼容旧本地记录；完成会话与未来 PK 题组携带完整版本快照；版本升级只能显式迁移。

**自动化测试：** 旧记录读取、版本缺失默认、版本冻结、升级迁移和不可重算断言。

**手动验收：** 创建旧/新版本夹具，核对历史显示和评分一致。

**验收标准：** 四类版本均有唯一来源、保存位置和兼容策略。

**本任务不做：** 不接入 Supabase 客户端，不创建 PK 页面。

### 任务 4.2：Supabase 表结构设计

**优先级：** 高。

**前置条件：** 任务 4.1 完成。

**当前问题：** 尚无经版本冻结约束的云端数据模型。

**目标行为：** 设计 profiles、training_sessions、question_records、question_sets、pk_challenges 等表及关联、索引和不可变字段。

**涉及模块：** 数据库设计文档、SQL 草案、迁移目录；不接入前端。

**数据与规则：** 所有训练、题组和题目记录保存四类版本；session UUID 作为幂等键；冻结题组和完成成绩应区分可写/不可写状态。

**自动化测试：** SQL 结构验证或临时数据库迁移测试。

**手动验收：** 审查主键、外键、唯一约束、索引和删除策略。

**验收标准：** 能表达本地所有必要数据及后续 PK，而不依赖重生成题目。

**本任务不做：** 不安装 Supabase 依赖，不写前端登录。

### 任务 4.3：Auth 方式设计

**优先级：** 高。

**前置条件：** 任务 4.2 完成。

**当前问题：** 现有 fish/cat 是本地训练身份，不等于可认证账号。

**目标行为：** 选择两个真实账号的登录、恢复、登出与账号切换方式。

**涉及模块：** Auth 方案文档、数据映射、隐私说明。

**数据与规则：** 明确本地 fish/cat 到认证 user ID 的迁移策略；禁止 service role key 出现在前端。

**自动化测试：** 设计登录成功、失效、登出和账号切换的集成测试案例。

**手动验收：** 评审账号恢复和换设备流程。

**验收标准：** 认证方式、失败提示和数据归属无歧义。

**本任务不做：** 不实际接入 Auth SDK。

### 任务 4.4：RLS 策略设计

**优先级：** 最高。

**前置条件：** 任务 4.2、4.3 完成。

**当前问题：** 没有数据库层访问边界，未来不能只依赖前端隐藏按钮。

**目标行为：** 写出训练、题组和挑战的最小权限策略。

**涉及模块：** RLS SQL 草案、权限矩阵、测试案例文档。

**数据与规则：** 🐟只能写自己的训练；🐱同理；双方只能读相关 PK；不能改对方成绩、冻结题组；未参与者不能读挑战；前端无 service role key。

**自动化测试：** 为每张已设计的表建立允许/拒绝 SQL 集成用例；后续每完成一张表和一个写入流程就立即运行对应 RLS 测试，不能等到 PK 全部完成后才首次验证。

**手动验收：** 权限矩阵逐项评审。

**验收标准：** 所有表均默认拒绝，只有必要的最小策略被允许。

**本任务不做：** 不启动云端同步或 PK UI。

### 任务 4.5：云端迁移 SQL 与回滚方案

**优先级：** 高。

**前置条件：** 任务 4.4 完成。

**当前问题：** 数据模型和权限若没有受控迁移与前向修复策略，首次上云风险不可控；破坏性数据迁移也不一定适合自动逆转。

**目标行为：** 形成可执行、可审查、可前向修复的迁移顺序，并为破坏性变更规定备份和测试环境演练。

**涉及模块：** SQL migration、回滚文档、部署检查表。

**数据与规则：** 先建表、索引、约束、RLS，后建策略；迁移不应破坏本地数据；规则版本字段不可遗漏。测试项目可重置；涉及删列、数据变换或约束收紧时，执行前备份并提供前向修复方案，不承诺危险的通用 down migration。

**自动化测试：** 空库升级、重复执行保护、前向修复、每表 RLS smoke test；破坏性迁移在隔离项目演练备份与恢复。

**手动验收：** 在隔离测试项目演练升级和回滚。

**验收标准：** SQL 可从空库完成部署；每个迁移都有测试环境演练、风险说明和前向修复路径。

**本任务不做：** 不向真实用户数据迁移，不做前端页面。

---

## 阶段 5：本地优先云同步

### 任务 5.1：Supabase 客户端接入

**优先级：** 高。

**前置条件：** 阶段 4 SQL 与权限在测试环境验证。

**当前问题：** 本地应用尚不具备受控云端连接。

**目标行为：** 以环境变量和受限客户端接入 Supabase，不影响无网本地训练。

**涉及模块：** 客户端封装、环境示例、错误边界、文档。

**数据与规则：** 只使用匿名公钥与用户令牌；禁止 service role；连接失败不阻塞 IndexedDB。

**自动化测试：** 配置缺失、客户端错误和离线降级。

**手动验收：** 无配置和有配置环境均有可理解行为。

**验收标准：** 云端访问集中封装，无未使用占位代码。

**本任务不做：** 不自动上传或展示 PK。

### 任务 5.2：两个账号登录

**优先级：** 高。

**前置条件：** 任务 5.1 完成。

**当前问题：** 本地用户选择尚未绑定云端身份。

**目标行为：** 两个实际账号可登录、恢复、登出和切换，且本地数据归属明确。

**涉及模块：** 登录界面、认证状态、身份映射、错误提示。

**数据与规则：** 账号切换不能把待同步记录上传到错误账号；登录失效不能丢失本地数据。

**自动化测试：** 登录成功、登录失败、过期、登出、账号切换。

**手动验收：** 两个账号分别登录并确认历史隔离。

**验收标准：** 本地身份和认证身份映射可追溯。

**本任务不做：** 不发起 PK。

### 任务 5.3：已完成训练手动同步

**优先级：** 高。

**前置条件：** 任务 5.2 完成。

**当前问题：** completed 会话只能留在本机。

**目标行为：** 用户可手动上传一个已完成训练及其逐题记录。

**涉及模块：** 同步服务、历史操作入口、云端映射。

**数据与规则：** session UUID 为幂等键；先创建会话再写逐题记录；保留版本字段和冻结题组数据。

**自动化测试：** 成功上传、重复上传不重复、训练上传成功但单题写入失败的可恢复状态。

**手动验收：** 上传一条训练后，重试不产生重复记录。

**验收标准：** 本地训练永远保留，云端成功状态可见。

**本任务不做：** 不自动重试，不同步 active 会话。

### 任务 5.4：待同步队列

**优先级：** 高。

**前置条件：** 任务 5.3 完成。

**当前问题：** 失败训练没有明确的本地待同步状态。

**目标行为：** 在 IndexedDB 中维护可恢复、可去重的完成会话同步队列。

**涉及模块：** 存储、同步服务、类型与历史状态展示。

**数据与规则：** 队列保存 session ID、账号、尝试次数、最后错误、更新时间；不复制完整业务数据；账号切换时不跨账号处理。

**自动化测试：** 入队、去重、成功出队、失败保留、旧数据兼容。

**手动验收：** 断网完成训练后可看到待同步，并在恢复后仍可处理。

**验收标准：** 相同 session 不会产生多条队列任务。

**本任务不做：** 不实现后台常驻同步。

### 任务 5.5：自动补传

**优先级：** 中高。

**前置条件：** 任务 5.4 完成。

**当前问题：** 待同步队列仍需用户逐条操作。

**目标行为：** 网络恢复、应用启动或用户触发时安全重试队列。

**涉及模块：** 同步调度、网络状态、应用内状态提示。

**数据与规则：** 指数退避或有限重试；不在训练中阻塞计时；成功幂等；失败保留原因。

**自动化测试：** 断网恢复、重复触发、部分失败、重试上限、登录失效。

**手动验收：** 离线完成 → 联网 → 自动补传，且不重复上传。

**验收标准：** 队列最终一致且用户能理解失败状态。

**本任务不做：** 不同步 active 训练或草稿。

### 任务 5.6：跨设备历史读取

**优先级：** 中高。

**前置条件：** 任务 5.5 完成。

**当前问题：** 云端数据未被第二台设备读取。

**目标行为：** 登录同一账号后读取并合并本地/云端 completed 历史。

**涉及模块：** 历史数据服务、合并策略、趋势数据源。

**数据与规则：** 按稳定 UUID 去重；云端与本地同一记录不重复计入趋势；版本不兼容应明确提示。

**自动化测试：** 同 ID 合并、不同设备历史、冲突数据、离线回退和趋势去重。

**手动验收：** 两台设备登录同账号后确认历史和趋势一致。

**验收标准：** 已同步 completed 记录跨设备可见，未同步本地记录不丢。

**本任务不做：** 不展示 PK 数据。

### 任务 5.7：同步失败和幂等测试

**优先级：** 最高。

**前置条件：** 任务 5.6 完成。

**当前问题：** 同步的失败、重复和冲突路径最容易损坏成绩数据。

**目标行为：** 完成稳定 UUID、待同步队列、断网恢复、登录失效、账号切换、重试和云端冲突的集成验证。

**涉及模块：** 同步服务、存储、认证、集成测试环境。

**数据与规则：** 训练上传成功但单题失败可重试；重复上传不重复；不同账号绝不混传；云端已完成成绩不可被本地旧副本覆盖。

**自动化测试：** 上述全部失败路径和幂等路径。

**手动验收：** 通过网络切换、退出登录和双设备重复操作复核。

**验收标准：** 任意中断后数据可恢复、可解释且无重复。

**本任务不做：** 不实现挑战胜负。

---

## 阶段 6：双人异步 PK

### 任务 6.1：冻结题组

**优先级：** 高。

**前置条件：** 阶段 5 通过同步失败与幂等测试。

**当前问题：** 同题公平性尚无云端冻结对象。

**目标行为：** 冻结完整题目、顺序、子模式、误差规则、题量和全部规则版本。

**涉及模块：** question_sets 表、PK 服务、版本字段。

**数据与规则：** 不依赖随机种子重生成；冻结后不可修改；引用 generator/grading/rating/schema 四类版本。

**自动化测试：** 冻结内容完整、重复请求幂等、修改拒绝。

**手动验收：** 发起前后比对题组 JSON 与顺序。

**验收标准：** 两名用户读取到相同 `questionSetId`、题目数量、题目顺序、题目 ID/内容、判题规则版本和规范化内容哈希；不以 JSON 字段序的字节等价作为业务标准。

**本任务不做：** 不做挑战列表 UI。

### 任务 6.2：发起挑战

**优先级：** 高。

**前置条件：** 任务 6.1 完成。

**当前问题：** 完成训练后无法创建待挑战记录。

**目标行为：** 创建与发起人成绩、冻结题组和目标用户关联的挑战。

**涉及模块：** 挑战服务、发起入口、数据库表。

**数据与规则：** 同一来源会话不重复发起有效挑战；状态初始为待接受；不改变个人历史。

**自动化测试：** 成功发起、重复点击、无效会话、冻结题组缺失。

**手动验收：** 发起后目标账号可见挑战元信息。

**验收标准：** 创建记录完整且幂等。

**本任务不做：** 不在此任务接受挑战或计算胜负。

### 任务 6.3：接受或跳过挑战

**优先级：** 高。

**前置条件：** 任务 6.2 完成。

**当前问题：** 目标用户不能对待挑战做明确决策。

**目标行为：** 目标用户可接受同题训练或跳过，且状态可追踪。

**涉及模块：** 挑战列表/详情、挑战状态服务。

**数据与规则：** 仅目标用户可接受/跳过；跳过不影响双方个人历史；接受后挑战绑定挑战者。

**自动化测试：** 接受、跳过、重复操作、非目标用户拒绝。

**手动验收：** 两账号验证状态变化和可见性。

**验收标准：** 状态转换单向、可解释且无重复挑战者。

**本任务不做：** 不在列表页比较逐题详情。

### 任务 6.4：挑战者完成同题训练

**优先级：** 高。

**前置条件：** 任务 6.3 完成。

**当前问题：** 接受后尚无训练会话与冻结题组的绑定。

**目标行为：** 挑战者从冻结题组创建本地训练，完成后按本地优先规则同步。

**涉及模块：** 训练创建、同步、挑战关联字段。

**数据与规则：** 不允许重新生成/改变题序；后台暂停、重开和计时规则与单人训练一致；结束会话永久关联 challenge ID。

**自动化测试：** 冻结题组复用、断网完成、重复提交和同步恢复。

**手动验收：** 对比双方题面、题序和规则版本。

**验收标准：** 挑战者成绩与题组可审计地关联。

**本任务不做：** 不在本任务定义 UI 排名。

### 任务 6.5：胜负计算

**优先级：** 高。

**前置条件：** 任务 6.4 完成。

**当前问题：** 还没有可复现的 PK 结果规则。

**目标行为：** 按正确题数、总有效用时、平局的既定顺序生成不可篡改结果。

**涉及模块：** PK 规则服务、数据库计算字段、详情展示数据。

**数据与规则：** 正确题数优先；相同再比较总有效用时；仍相同为平局。规则版本随挑战冻结，不能被后续评级规则重算影响。

**自动化测试：** 三种胜负分支、相同成绩、异常/不完整成绩拒绝、重复计算幂等。

**手动验收：** 预置两份结果，核对胜者。

**验收标准：** 同一挑战任意设备读取结果一致。

**本任务不做：** 不增加积分、奖励或排行榜。

### 任务 6.6：PK 列表页

**优先级：** 中。

**前置条件：** 任务 6.5 完成。

**当前问题：** 用户没有集中查看待挑战、已跳过和已完成 PK 的入口。

**目标行为：** 按状态显示与当前用户有关的挑战。

**涉及模块：** PK 列表组件、查询服务、空/错误状态。

**数据与规则：** 仅显示本人可访问挑战；不泄露未参与者数据。

**自动化测试：** 状态分组、空列表、加载失败、权限过滤。

**手动验收：** 两账号分别查看同一挑战的合理视图。

**验收标准：** 列表状态与数据库一致。

**本任务不做：** 不做逐题对比。

### 任务 6.7：PK 详情和逐题对比

**优先级：** 中。

**前置条件：** 任务 6.6 完成。

**当前问题：** PK 结果缺少可复盘依据。

**目标行为：** 展示双方总成绩、逐题答案、正确结果、正误和耗时；旧记录若含单题重开次数仍可读取，但详情不再展示该列。

**涉及模块：** PK 详情组件、只读查询、已有 `SessionDetails` 的复用边界。

**数据与规则：** 仅双方可见；显示冻结题组和版本；不得允许编辑成绩或题目。

**自动化测试：** 正确映射逐题数据、无权限拒绝、缺失记录提示。

**手动验收：** 两账号打开同一详情，确认读到相同结果。

**验收标准：** 胜负可以由详情数据复核。

**本任务不做：** 不做公开分享或排行。

### 任务 6.8：RLS 和篡改防护测试

**优先级：** 最高。

**前置条件：** 任务 6.7 完成。

**当前问题：** 每张表的基础 RLS 已应在阶段 4/5 即时验证；PK 页面完成后仍需要覆盖挑战组合、冻结题组和逐题成绩的完整回归。

**目标行为：** 在真实 Supabase 测试环境完成 PK 相关 RLS 和不可变约束的端到端回归，而不是第一次测试 RLS。

**涉及模块：** RLS SQL、数据库约束、集成测试。

**数据与规则：** 🐟只能写自己训练；🐱同理；双方仅访问相关 PK；不能改对方成绩、冻结题组；未参与者不可读取；前端无 service role key。

**自动化测试：** 每条允许/拒绝规则、越权读写、冻结后修改、匿名访问。

**手动验收：** 使用两个参与账号和一个未参与账号复测。

**验收标准：** 所有越权操作均被数据库拒绝，而非仅 UI 隐藏。

**本任务不做：** 不增加多人好友关系。

---

## 阶段 7：跨设备完整验收

### 任务 7.1：双设备、同步、PK 端到端验收

**优先级：** 最高。

**前置条件：** 阶段 6 全部完成并通过 RLS 测试。

**当前问题：** 单机、集成测试和数据库权限通过后，仍需真实设备验证完整产品链路。

**目标行为：** 用两部真实手机、两个账号验证训练、同步、PK、离线和更新的全链路。

**涉及模块：** 无新增业务功能；测试计划、问题记录和发布检查表。

**数据与规则：** 覆盖同题同序、断网训练、联网补传、重复提交、登录过期、账号切换、权限越权、冻结题组不可修改、PK 胜负一致和 PWA 更新后旧数据兼容。

**自动化测试：** 保留阶段 2 E2E、阶段 5 集成、阶段 6 RLS；本任务以真实双设备手动验收为主。

**手动验收：** 对每个覆盖项记录设备、账号、网络状态、步骤、结果和证据。

**验收标准：** 无 P0/P1 数据一致性、权限、冻结或胜负问题；遗留问题明确分级。

**本任务不做：** 不新增功能，不以“顺手修复”代替验收结论。

---

## 阶段 8：可选项

### 任务 8.1：Capacitor Android 封装

**优先级：** 可选。

**前置条件：** 阶段 7 完成且 PWA 满足日常使用。

**当前问题：** PWA 若无法满足安装/原生体验，才需要 Android 容器。

**目标行为：** 评估并封装现有 Web 应用为 Android 安装包。

**涉及模块：** Capacitor、Android 配置、真机发布测试。

**数据与规则：** 不重写题库、同步或 PK；保持本地优先数据行为。

**自动化测试：** 构建和基本启动验证。

**手动验收：** 安装、更新、前后台、草稿和同步复测。

**验收标准：** 仅在比 PWA 有明确收益时推进。

**本任务不做：** 不上架应用商店作为默认目标。

### 任务 8.2：V2 资料分析公式题

**优先级：** 可选。

**前置条件：** 基础题库和 PK 已稳定。

**当前问题：** V1 只训练列式后的基础算力。

**目标行为：** 单独立项设计基期量、增长量、比重、平均数等公式训练。

**涉及模块：** 新 PRD、新生成器、新评分与版本策略。

**数据与规则：** 不复用 V1 随机题规则直接扩展；先冻结新的训练目标。

**自动化测试：** 新题型独立逻辑、题库与评分测试。

**手动验收：** 由资料分析训练目标评审。

**验收标准：** V2 不破坏 V1 历史解释。

**本任务不做：** 不在 V1 计划内插入公式题。

### 任务 8.3：完整选项式资料分析训练

**优先级：** 可选。

**前置条件：** 任务 8.2 独立立项并完成。

**当前问题：** 基础速算与真实材料题之间仍有训练层级差。

**目标行为：** 设计带材料、选项精度和策略判断的完整训练。

**涉及模块：** 新内容模型、题目来源/授权、阅读和作答界面。

**数据与规则：** 必须另行明确真题版权、题库来源和判题标准。

**自动化测试：** 材料/选项数据完整性与答题流程。

**手动验收：** 以真实考试流程评估。

**验收标准：** 作为独立产品模块验收。

**本任务不做：** 不混入现有 9 类基础题的生成器。

### 持续明确不做

- AI 手写识别。
- 自动错题讲解。
- 公开排行榜。
- 多人好友系统。

## 下一步第一项任务

**任务 2.1：清理旧渲染分支。**
阶段 1 的九类基础题库已完成结构化，分数比较也已移除等值题；下一步按计划稳定本地工程结构，不提前进入 PWA、Supabase、同步或 PK。
