# 高频数据页加载审计（2026-08-04）

## `08e0601` 没有生效的原因

`08e0601` 虽然增加了缓存和“先显示、后更新”的意图，但仍在每次进入历史、成绩、详情或 PK 路由时调用刷新函数。历史刷新先串行读取 IndexedDB，再把 `history` 和缓存直接替换为本地集合；登录用户的新设备本地集合通常为空，先前已合并的云端记录因此被清空，随后才开始云端读取。PK 刷新也先读 completed，并且无条件读取完整云端历史，即使 PK 首屏只需要挑战列表。

结果是缓存会被写入，却没有在下一次刷新中作为稳定渲染源保留；每次路由又重新开始相同读取，`historyInitialLoading`/`pkInitialLoading` 便把页面替换成整页占位。返回首页虽未主动清除 Map，但重新进入的刷新链路让体验与没有缓存几乎相同。

## 本轮读取链路

1. Auth 范围确认后，首页后台启动当前范围的数据预取；未登录只读本地 completed，已登录并行启动 IndexedDB 与允许读取的云端历史。
2. `historyScope` 以 Auth UUID 或 `local-unassigned` 隔离缓存、正在进行请求和可渲染结果。
3. 本地结果先与旧缓存合并，而不是覆盖旧缓存；云端结果返回后再合并。相同 ID 保留本地会话，以确保本地同步状态不被旧云端副本覆盖。
4. 同一范围的历史或 PK 读取只保留一个 Promise。首页预取、首次路由和手动刷新复用该 Promise。
5. 路由已命中缓存时不再自动重新读取。PK 首屏只读取挑战列表；蓝色结果提醒使用独立确认调用，不触发完整挑战/历史重新加载。
6. 成绩页只构造当前选择的题型与子模式趋势图，其他趋势轨道在用户切换时再构造，避免一次性渲染全部图表阻塞首帧。

## 开发期测量

开发环境控制台会输出 `[data-load]` JSON 事件：`navigate`（含缓存命中）、`history-start`、`history-local-ready`、`history-cloud-ready`、`history-reuse`、`pk-start`、`pk-ready` 和 `pk-reuse`。其中的 `elapsedMs` 用来对照本地首批内容与云端更新完成时间；生产环境不会输出这些记录。

本地浏览器验证（未配置 Supabase 的开发环境）确认：主页进入历史与成绩时立即显示对应标题、筛选/趋势结构和真实空状态，连续返回并再次进入不再出现“正在准备…”整页占位；日志显示第二次导航为缓存命中，未产生新的 history 请求。PK 的真实账号云端请求需要在已配置生产环境中按下列清单复验，开发机不会使用或要求真实账号凭据。

## 真机验收清单

- 登录后等待首页完成后台预取；依次执行“首页 → 我的成绩 → 首页 → 我的成绩”，第二次进入不出现整页加载。
- 对“历史记录”和“PK挑战”重复上述操作；网络请求进行时只允许小型刷新提示，已有卡片、筛选和页码必须保留。
- 在成绩、历史、PK 之间连续切换，确认各页标题和已有内容先出现，后台更新不清空内容。
- 以慢网和断网分别验证：本地已有数据时仍先展示；云端失败仅显示更新提示。
- 退出并切换另一账号后重复操作，确认不会看到前一账号的历史、统计、PK 卡片、筛选或页码。
