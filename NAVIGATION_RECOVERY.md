# 页面导航与刷新恢复

产品名称统一为“速算训练”。页面使用可读 hash 路由：`#/`、`#/history`、`#/stats`、`#/pk`、`#/training`，以及带稳定记录 ID 的 `#/history/{id}`、`#/pk/{id}`、`#/result/{id}`。应用内导航写入浏览器历史，浏览器返回/前进重新解析 URL；新页面应沿用这一模式，不依赖只存在于 React 内存的页面位置。

## 登录三态

1. **确认中**：不显示未登录表单，只显示“正在确认账号状态”。
2. **已登录**：显示绑定的🐟/🐱身份、紧凑邮箱与退出入口。
3. **已确认未登录**：默认显示“未登录，仅本地保存”和登录入口；只有点击后展开完整邮箱、密码表单。

## 刷新边界

- 首页、历史、成绩趋势和 PK 页按 URL 保持功能位置，读取期间显示加载状态而非空列表。
- 历史详情、PK详情和结算页使用 URL 中的稳定 ID 重新读取冻结记录；不存在、无权限、过期和读取失败均显示明确说明，不静默回首页。
- `#/training` 仅恢复当前账号在本浏览器的 active 训练，暂停离开间隔后继续原冻结题组、进度、答案、用时和 PK 来源；不存在时明确提示，不重生成。
- `#/result/{id}` 只读取既有 completed 训练，绝不重新完成、同步、提交 PK 或创建记录。无法恢复时提供历史入口。
- 题量弹窗、登录表单展开、草稿开关、确认框和临时错误不作为导航恢复状态。

## 手机首页原则

首页先展示“速算训练”、紧凑账号状态和三个核心入口（我的成绩、历史记录、PK挑战），再进入题型选择。登录表单按需展开，账号身份仍完全由真实 Auth 账号决定；PK 红蓝气泡规则不随布局改变。
